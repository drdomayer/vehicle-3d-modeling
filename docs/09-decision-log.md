# 09 — Decision log

Формат: дата — решение — причина. Добавяй отгоре. Не трий стари редове.

- 2026-09-26 (3) — **Ревю на собствените ходове: една неточност в записа, един бъг в листа, и
  документът, който печатницата няма.** (1) В комита за люспите бях написал като факт, че „счупи го
  събирането на езичето" — **не съм го проверил**. По-вероятният механизъм е, че ъгловата люспа е
  отделен остров на черупката в собствената си клетка, свързан с останалото само през равнината на
  среза; сливането ѝ в „съседа с най-много лица" не я свързва, ако той не е клетката, през която тя
  се закача. Записът вече казва „не е установено". (2) `fit_test` вземаше ориентирите от **всички**
  върхове, включително вътрешното лице на стената и езичетата — „най-предна точка" на вътрешната
  страна на 3 mm черупка не е нещо, до което се мери. Сега само външната повърхност, със същия
  радиален тест като производството; три от шестте ориентира се преместиха. (3) В `03_PRINT/`
  имаше STL-и, матрица и CSV, и **нищо, което една печатница да прочете**: каква стена, какъв
  материал, какво да провери. Написано на ръка би остаряло за седмица — урокът от `check_reports`.
  Затова `panel_production` записва решенията и бройките като данни в `handoff.json`, а
  `print_qc` — последната стъпка, която знае QC резултата — генерира `03_PRINT/README.md` от тях.
  Двата файла са под надзор за възраст. **23 доклада current, 0 stale.**

- 2026-09-26 (2) — **28% от секциите са люспи, и моят опит да ги слея направи нещата по-лоши.**
  Измерено по целия комплект: **149 от 531 секции са под 40 mm** по най-голямото си измерение,
  а **68 са под 20**. Дванайсетмилиметрова люспа от 3 mm стена не е част — не се хваща, не се
  подравнява, не се лепи, и печатница, на която дадеш 149 такива, ще изгуби част от тях.
  Най-зле са `P21` (45), `P01` (18) и `P03/P04` (по 17).
  Причината не е бъг, а геометрия: **3D решетка върху тънка извита черупка оставя ъглова отломка
  във всяка клетка, която черупката само докосва**, и никаква стъпка не я премахва.
  **Опитът за сливане: 311 секции станаха 623, а 440 от тях под 60 mm.** Формата на грешката си
  струва да се запази. Сливането трябва да е на **групи клетки**, не чрез слепване на обекти,
  защото равнината между две слети клетки спира да е срез, а езиче, изтеглено там, би вдлъбнало
  жлеб в средата на непрекъснат панел — тази част беше вярна. Счупи го събирането на езичето: лице
  извън групата се приемаше, ако е в рамките на едно езиче от **коя да е** клетка на групата, а не
  от собствената ѝ външна граница — значи се дърпаше далечен материал, секциите излизаха несвързани
  и `loose_pieces` цепеше всяка на няколко.
  Върнато. **Истинският лек вероятно не е 3D решетка изобщо** — черупка иска рязане по двете си
  собствени повърхностни посоки, не по трите световни оси, и това е по-голяма промяна от сливане.
  Втори път днес моя поправка излиза по-лоша от проблема; и двата пъти я хвана мярка, а не око.

- 2026-09-26 — **Лявата и дясната страна на симетрична кола се режеха на различен брой части.**
  `P07 ROCKER_L` даваше **34 секции, `P08 ROCKER_R` — 18**, при идентични мешове (366 върха,
  314 лица, еднакъв bbox до 0.1 mm) и идентичен план за рязане (5 среза по X, 1 по Y).
  Причината е в теста за принадлежност към клетка: клетка взима лицата на съседа си като **езиче
  само в ПОЛОЖИТЕЛНАТА посока на всяка ос** — `hi <= cc < hi + tab`. Тази посока е абсолютна, значи
  отляво езичето стига навън, а отдясно навътре, и тънка клетка, която оцелява при
  `len(faces) < 3` от едната страна, отпада от другата.
  Да направя посоката относителна би пренаписало всеки съществуващ файл. Огледалото е и по-малка,
  и по-вярна промяна: колата е симетрична по построение, `panel_map` вече строи дясната половина
  като огледало на лявата, а две страни, които се различават, са производствена неприятност, не
  дизайн. **Дясната секция вече е огледало на лявата, а не втори независим срез.**
  Резултат: **12 от 12 огледални двойки съвпадат** (бяха 10 от 12). И странично, но същественo:
  **ширината на сглобеното пада 1852.2 → точно 1850.0** при заключени 1850 — тоест остатъкът от
  2.2 mm не беше „езичето по дизайн", както бях записал, а същата асиметрия, бутаща материал
  отвъд заключен размер отдясно. **Оттеглям обяснението от 21 септември.**

- 2026-09-26 — **`fit_test.py` — CLAUDE.md твърдо ограничение 6 най-после има съдържание.**
  „Не позволявай на собственика да пропусне fit-test на един отпечатан панел, преди да се печата
  останалото" стоеше като правило, но нищо не казваше какво МЕРИ тестът и какво значи да мине.
  Панелът е избран **от данните**: изцяло наш (значи лош резултат обвинява процеса, не липсващ
  скан), малък и евтин, с реална кривина в две посоки, в повече от една секция (иначе фугата с
  езиче остава непроверена) и на козметично прощаващо място. **`P39 ROCKER_END_L`** — 0.080 m²,
  0.30 kg, 3 секции, зад задното колело.
  Един печат отговаря на шест допускания, всяко от които е неотговорен въпрос от `docs/13`: държи
  ли 3 mm стена ламинат; държи ли езичето двете секции в линия; печата ли се на ориентацията, която
  веригата е избрала; заравят ли кит и боя характерните линии; колко свива материалът; и съвпада ли
  готовият ПАНЕЛ с проектната повърхност — `assembly_check` доказва, че ФАЙЛОВЕТЕ съвпадат до
  медиана 0.00 mm, което е друго твърдение.
  Мери се **с шублер и линия, без скенер**: шест ориентира, всеки краен по своята ос, тоест ъгъл
  или връх, върху който можеш да опреш челюст, и таблица с разстоянията между тях. Мери се на
  четири етапа и **промяната на всеки етап казва кой етап я е направил**.
  Какво значи „минава" **не се измисля**: допуск от никого няма (`docs/13` Q16), затова листът дава
  мащаба, който останалата работа вече допуска — фугите са 4 mm, фланецът 1 mm лепило, колата е
  заключена на 1850. Числата се записват и решението се взима с фирмата.

- 2026-09-21 — **Стопът — най-разпознаваемият елемент на задницата — не съществуваше в геометрията.**
  `docs/14` го заключва като *„тънко широко хоризонтално острие през задницата, с остри L-образни
  външни краища"* и изрично НЕ като триъгълна супер-кар лампа. Обвивките стояха в скелета от
  14 септември и `check_lighting` ги отчиташе като заградени — но **нищо не беше изрязано за тях**:
  `check_packaging` чете `TAIL_BAR` на **115.5 mm ПОД повърхността** и `TAIL_END` на 20.6. Това е
  вярно за кухина и безполезно без отвор.
  Построено по вече валидирания модел на фара: прорез до повърхността плюс корпус с 5 mm стена и
  6 mm въздух около модула, отворен назад, защото **E-маркираният модул не се пипа**. Всички числа
  идват от LOCKED обвивките — `TAIL_BAR` specX 3200, 45×1440×38; `TAIL_END` 3120, ±700, 120×40×38 —
  не от преценка. Одит: **21/2/13/6 → 23 PROCEED / 2 / 13 / 4.**

- 2026-09-21 — **Първият ми опит за L-образния корпус даде две несвързани парчета, и `panel_extract`
  го каза.** Направих два `shell()`-а и ги обединих — но `shell()` връща **отворено** тяло, едно лице
  нарочно липсва, а EXACT union на два отворени меша не свързва надеждно. Верният ред: обединяваш
  **затворените външни** кутии, обединяваш **затворените вътрешни**, вадиш веднъж. Всяка булева
  операция тогава върви върху плътно тяло. P26/P27 стават по едно парче, 786.6 cm³.

- 2026-09-21 — **P23 REAR_SPOILER: НЕ се строи, и мярката казва защо, а не предпочитание.**
  `check_packaging` слага обвивката му **125.7 mm под повърхността и 108.8 навътре** при specX 3120 —
  тя е изцяло погребана в тялото. Острие, построено там, стои вътре в каросерията. Това съвпада с
  вече записаното решение „spoiler-ът стана ducktail": формата е минала в собствения профил на
  опашката, където `TAIL_DROP` я носи. **P23 е остатък в регистъра, не част, която чака моделиране** —
  и се решава в регистъра, не с геометрия.
  Резултат: 505 файла, 11 дефектни (2%), покритие **76.2% → 76.7%**, осемте метрики плоски,
  осветление 0 проблема.

- 2026-09-21 — **Non-manifold ръбовете са проследени до ПРИЩИПНАТ ВЪРХ, и съвпадението е едно към
  едно.** Броят върхове, в които се срещат повече от два гранични ръба, е точно равен на броя
  non-manifold ръбове, които стената после произвежда: P01 **2 и 2**, P21 **9 и 9**, P07/P22/P28
  **0 и 0**. Извлеченият регион се допира сам до себе си в точка, `solidify` прекарва ръба си през
  нея два пъти и се получава ръб с четири лица.
  Разделянето не е заобикаляне, а честната форма: **две площи, съединени в една точка, не могат да
  се отпечатат като една част** и биха се разпаднали. Веригата вече изнася всяко свободно парче
  отделно, значи излизат като двете части, които са. Обемът мръдва с 0.04%, `|Y|max` — с нищо.
  **105 от 315 дефектни → 15 от 497.**

- 2026-09-21 — **Два обърнати наопаки файла, и поправката ми не подейства от първия път.**
  `P13/P14_INTAKE_BLADE_*_s01b` излизаха с **обем −700.9 cm³** — слайсер би отпечатал допълнението
  им. Причината е в `face_outward`: за **затворено** тяло радиалният тест „навън от оста на колата"
  е догадка, а знакът на обема е точният отговор. Смених го — и нищо не се промени, защото тестът
  се правеше на цялата **секция**, а разделянето на парчета идва след него: секция с две парчета
  може да има положителен сумарен обем, докато едното е обърнато. Преместено след разделянето, на
  всяко парче поотделно. **15 → 8 от 497.**

- 2026-09-21 — **Остават 8 файла от 497 с по 1–11 non-manifold ръба.** Те преживяват
  `split_pinch`, значи са друг механизъм, не прищипнат връх. Не са гонени днес — вече една моя
  поправка се оказа по-лоша от проблема, и не тръгвам след трета в същия ден без да я премеря.

- 2026-09-21 — **`print_qc.py`: никой никога не беше отварял отпечатаните файлове.** Веригата
  проверява дали секция влиза в плота и дали файлът е едно свързано парче. Нито едното не е
  въпросът, който слайсерът задава: той иска **затворено, манифолд тяло с последователна навивка и
  положителен обем**. Написан извън Blender нарочно — чете файловете, които ще се изпратят, не
  обектите, от които идват.
  **Първо одитирах одитора и той беше сгрешен:** квантоването беше `1e-4` „метра" върху файлове в
  **милиметри**, тоест праг 0.0001 mm, а float32 при 900 mm има стъпка ~1.07e-4 mm — прагът стоеше
  под шума на самия формат. (Поправено на микрон. Резултатът се оказа същият, тоест дефектите бяха
  истински — но проверката беше задължителна.) И броеше **един дефект два пъти**: ръб с три лица
  задължително има две, които го обхождат еднакво, значи „inconsistent winding" беше същото нещо.

- 2026-09-21 — **`cap_cuts` беше по-лошо от това, което замени. Оттеглено същия ден.** Сравнено на
  една и съща секция на P01: след рязане 446 отворени ръба; **моята `cap_cuts` — 13 отворени,
  352 non-manifold, обем 3 688 487 cm³** (безсмислен); старото второ удебеляване — 0 отворени,
  16 non-manifold, 924 837 cm³. `holes_fill` върху 3 mm широките гранични контури на черупка прави
  застъпващи се лица, а `triangle_fill` върху остатъка свързва несвързани ръбове напряко.
  Тоест поправих 3.5 mm, като счупих телата, и го пуснах, без да проверя, че са още тела. Точно
  затова `print_qc` вече съществува.

- 2026-09-21 — **Верният ред: реже се ПОВЪРХНОСТТА, стената се слага веднъж, на ниво секция.**
  Панелът се удебеляваше пръв, значи всяка секция беше парче от плътно тяло и трябваше да се
  запушва — а `solidify` не знае, че го молят да запуши дупка. Ако се реже повърхността, едно
  удебеляване с нормали навън затваря секцията **по построение**, върви навътре и оставя външното
  лице точно там, където дизайнът го е сложил.
  Измерено на една секция: P01 **924 837 → 460 478 cm³**, P07 **464 439 → 240 065**; non-manifold
  P01 16 → 9, P07 5 → 2; `|Y|max` ляга точно на проектната повърхност.
  **Поправка на собственото ми първо заключение:** казах „всяка маса, която този скрипт е докладвал,
  е била два пъти завишена". **Не е.** Масата се смята от удебеления ПАНЕЛ, който винаги е бил с
  една стена, значи докладваното число е било вярно. Грешното е по-лошо: **файловете щяха да изядат
  около два пъти материала, който графикът обещава.**
  Резултат по целия комплект: **349 от 417 дефектни файла → 105 от 315**, и остатъкът е само
  non-manifold ръбове — нула отворени ръбове, нула отрицателни обеми, нула изродени триъгълници.

- 2026-09-21 — **Остатъчният дефект е проследен до `solidify` и НЕ е поправен днес.** Извлечените
  повърхности са чисти (0 non-manifold, 0 дублирани върха); стената ги внася — P01 2, P21 9, P07 0.
  Два опита за лек, и двата отхвърлени с числа: режим `NON_MANIFOLD` маха дефекта (2→0, 9→0), но
  **взривява P01 до 1711 mm полуширина**; сливане по разстояние на 0.01 и 0.05 mm не променя нищо,
  значи ръбовете не са съвпадащи върхове, а структурни — черупката се прегъва върху себе си.
  105 файла носят по 1–20 такива ръба от няколко хиляди. Повечето слайсери ги поправят, но
  „повечето слайсери" не е гаранция и не се записва като такава. Верният лек е bisect, който
  запълва, или манифолд-осъзнат offset — **следваща задача, не днешна.**

- 2026-09-21 — **Поправката за нормалите НЕ подейства, и намирането защо даде истинския дефект:
  всяка секция се удебеляваше ВТОРИ път.** След `face_outward()` сглобената кола пак мереше 1856.9
  ширина. Вместо да донастройвам, проследих стъпка по стъпка на `P15`:
  извлечена повърхност **925.00**, след стената на панела **925.00** и 0 отворени ръба (тоест
  панелът вече е плътно тяло), след рязане на 31 секции **925.21** и 130 отворени ръба, след
  второто удебеляване — **928.14**.
  Тоест второто `thicken` съществува само за да **затвори срезовете**, но solidify не знае, че го
  молят да запуши дупка: той строи втора стена, и на външната повърхност я строи НАВЪН. 3.5 mm ядро
  извън заключените 925, с ламинат и кит още отгоре.
  Заменено с `cap_cuts()` — запълва граничните контури и не добавя материал. Проверено:
  ширина **1856.9 → 1851.1** при заключени 1850; остатъкът от 0.55 mm на страна е езичето, което по
  дизайн минава отвъд среза. Точността непроменена: медиана 0.00 mm, 99-и персентил 2.56, 99.4% в
  8 mm. Секции 226 → 203 производствени и 218 → 214 shape-only, всички едно парче.
  `face_outward()` остава — тя е вярна сама по себе си (P15 отива от 93.2% на 100% навън) и е
  нужна за първото удебеляване — но тя **не беше причината** и това е записано.
  Странично: STAGE03 частите също получаваха стена на ниво секция, макар да са плътни тела от
  `stage03_elements`; вече само им се оправят нормалите.

- 2026-09-21 — **Отпечатаните файлове не носеха запис къде отиват по колата — значи въпросът
  „1:1 ли е сглобено" беше непитаем.** `lay_flat` завърта секцията върху най-плоското ѝ лице, слага
  я на Z 0 и **запича трансформацията в меша**, после нулира матрицата. 248 файла без никакъв
  адрес: човекът, който ги лепи, трябва да ги подрежда по форма. Сега `lay_flat` връща матрицата,
  `print_schedule.csv` носи шест нови колони (ротация в градуси и точка в милиметри), а
  `03_PRINT/production/placement.json` носи точната 4×4.

- 2026-09-21 — **`assembly_check.py`: сглобявам обратно отпечатаните файлове и меря колата, която
  излиза.** Между главното тяло и файловете стоят шест операции — извличане, solidify 3 mm, рязане
  по решетка, езиче, разделяне на парчета и lay-flat — и никоя от тях не е била проверявана
  end-to-end. Резултатът дели въпроса на два, защото едно число за двата прави здрава верига да
  изглежда счупена:
  **Веригата е вярна.** Върху повърхност, която има файлове: медиана **0.00 mm**, 90-и персентил
  0.27, 99-и 2.49, **99.4% в рамките на 8 mm.**
  **Колата не е цяла.** Покритие 35.5% от външната обшивка — защото 20 от 42 части нямат файлове.
  Три мои грешки в самата проверка, намерени и поправени преди доклада: обявяваше 6 файла за
  липсващи, докато всичките 248 са на диска (`read_stl` връщаше `None` и за „нечетим", и за
  „нула триъгълника" — те наистина бяха празни, което е отделен дефект); мереше покритие срещу
  **цялото затворено тяло**, включително пода, стените на кабината и арките, които никой панел не
  бива да покрива (25.199 m² вместо 12.873); и изпълняваше `panel_map.py` два пъти, а той няма
  `__main__` guard, тоест изсипваше целия си доклад по средата.

- 2026-09-21 — **SHAPE ONLY: 10.9 m² форма, която е наша, не се произвеждаше.** Одитът маркира
  13 части SCAN REQUIRED и 2 CONDITIONAL, а производството пуска само PROCEED. Но трите причини не
  са едно и също нещо: **P09/P10 и P15/P16** са overlay — „вътрешната форма е обшивката на донора",
  тоест външната е наша; **P03/P04** — „границата мърда с `door_front_x`", тоест формата е наша,
  мърда мястото на изрязване; **P17–P20, P42** — „обем на сгъване на покрива, който не съществува
  като данни", тоест самата ФОРМА е неизвестна и построяването ѝ е измисляне.
  `CLAUDE.md` записва решението, което това изпълнява: *„цялата наша форма се строи сега, а сканът
  остава като последен fitting етап; монтажният интерфейс се строи ПОСЛЕДЕН."* Задържането им беше
  веригата да не прави каквото проектът вече е решил. Шестте отиват в **собствена директория със
  собствен график**, не са в пакета за доставчик, и всеки ред носи какво още липсва. Покритие
  **35.5% → 76.2%**.

- 2026-09-21 — **Стената на ядрото излизаше НАВЪН при част от панелите — 3.5 mm извън заключената
  ширина.** Сглобената кола мереше полуширина **928.52 mm при заключени 925**, при `P03_FRONT_FENDER`;
  производственото ниво стоеше точно на 925.00. Причината: `solidify` с `offset = -1` строи стената
  от страната, **обратна на нормалата**, а измерено — 5 до 15% от лицата на всеки извлечен панел
  гледат навътре (P01 84.9% навън, P21 90.1, P15 93.2, P03 94.7). Обърнато лице строи стената си
  навън, в въздуха, където панелът трябва да свършва — плюс ламинат и кит върху нея.
  `face_outward()` прави нормалите последователни и ги ориентира навън преди удебеляване;
  регионът е отворена черупка, значи последователността сама не казва накъде е „навън", и решението
  се взима по средното скаларно произведение с радиалната посока, претеглено по площ.
  **НЕ Е ПРОВЕРЕНО: Blender падна по време на пробега, който щеше да го измери.** Поправката е
  написана и обоснована, но числото 925.00 не е потвърдено. Първото нещо при следващо отваряне.

- 2026-09-21 — **v035: контролата, която написах вчера, имаше два свои бъга — и това е поуката.**
  `surface_probe.py` го въведох като мярката, която „същият трик не може да помръдне". Одитирах я и
  тя се оказа сгрешена на две места, и двете невидими отвътре:
  **1. Взимаше ПЪРВОТО пресичане на височина, след като сортира контура по Z.** Сечението обаче не е
  монотонно по Z — излиза навън и нагоре до гребена, после се прибира навътре и нагоре до короната —
  значи сортирането преплита двата клона и интерполацията свързва точка от единия с точка от другия.
  При specX −333 върна полуширина **31.5 mm** при Z 775, а съседните станции дават 511 и 730.
  **2. Преградата срещу почти хоризонтална повърхност искаше ДВЕТЕ съседни проби да съществуват,
  преди да тества наклона** — значи семпъл на по-малко от 10 mm под върха на сечението изобщо не се
  тестваше, а точно тези бяха най-лошите числа, които печаташе.
  Заедно надуваха средното около **четири пъти**, а най-лошото **петнайсет**: `|d²y/dx²|` четеше
  17.37, а отговорът е **4.71**; най-лошото 993 mm, а е **67**.
  **Оттеглям всяко `surf_*` число отпреди поправката, включително двете присъди REGRESSED, които тя
  произведе.** Премерени наново, и двете промени, които беше осъдила, са неутрални за повърхността:
  закотвяне на предния гребен 4.713 срещу 4.696, обединяване на долния ръб 5.48 срещу 5.37 —
  разлики под 0.05 mm при допуск 1.0.
  Поуката е по-обща от бъга: **контрола, която никой не одитира, е просто още едно число за вярване.**

- 2026-09-21 — **v035: долният ръб на тялото става ЕДНА котвена двойка.** Прагът върви до specX 1820,
  undercut-ът започва на 2260 — никога не съществуват едновременно, но `feature_anchors` слагаше и
  двете двойки на всяка станция: **четири котви в лента от 40 mm по Z, от които живи са най-много
  две.** Правилото за 1 mm разделяне после изтласкваше живата двойка от собствената ѝ черта с
  мъртвата: при specX 2750 инертните котви на прага стояха на 2.8 и 7.2 mm от undercut-а.
  Обединени: **undercut 46.5 → 69.5°** (дизайн 59.9), **праг 46.1 → 49.8°** (дизайн 49.6, тоест точно),
  долно рамо на канала 12.5 → 9.8 (дизайн 7.8), устна 66.8 → 68.2. Гребенът връща 3.6°, защото двата
  освободени семпъла се преразпределят; той пак е най-острата линия на колата и пак е записано
  изключение. Повърхността: неутрално. Панели: 248 от 248 секции едно парче, одит 21/2/13/6.

- 2026-09-21 — **v034: `edge_test` вече показва и страната на ДИЗАЙНА, и веднага намери регресия,
  която аз бях внесъл.** Всяко число за ръб досега е dihedral върху меша, а 2×2-то показа какво
  струва това само по себе си: прагът чете 12.2° или 47.7° за **един и същи дизайн**, според
  семплирането. Затова до измерената колона вече стои проектната — същият ъгъл, но върху профила
  **преди** resample, на стъпка 6 mm вместо 38. Разликата между двете е **загуба от представяне**,
  и тя има цена в производството: ядрото е мешът, значи ръб, който мешът не може да покаже, няма да
  го има и в ламинирания панел.
  Първото ѝ пускане даде: устна 66.9 меш / 65.7 дизайн (показана изцяло), праг 47.4 / 49.6
  (котвите работят), undercut 48.8 / 59.9 (губи 11.1°), и **преден гребен 38.6 / 55.2 — губи 16.6°,**
  най-голямата загуба на колата.
  Причината е моя: `resample_anchored` няма защитата на локалните максимуми, която `resample` носи
  от 16 септември (добавена, защото гребените пристигали 68 mm къси). Измерено сега — струва
  **2.1 mm средно, 4.4 максимум**, тоест малко, но реално. Гребенът обаче е и двуточкова черта
  26×30 mm, а мешът семплира на 38 — затова верният лек не е snap-ът, а **котва**: той е именно
  наименувана надлъжна линия и го бях пропуснал, защото се долепя след `pts`, а не е част от
  профила. Резултат: **44.2 → 52.0° при дизайн 55.5, загубата 11.3 → 3.5**, обем непроменен.
  **Следствие, което е находка, не дефект:** 52° е извън G1 лентата. Но дизайнът винаги е бил 55.5 —
  линията не е станала по-остра, а **видима**. Записано като изключение с причина, и то е **същият
  въпрос като прага**: две от G1 линиите на `docs/16` са проектирани на ~50°, значи или лентата
  8–40 е твърде тясна за кола с такъв език, или и двете линии са по-твърди от спецификацията.
  Един въпрос, не два, и е твой.

- 2026-09-21 — **Контролата се задейства при първото си истинско ползване и беше права.**
  `surf_x_mm` скочи 17.37 → 19.15 при закотвянето на гребена и `check_goal` обяви REGRESSED, при
  непроменен обем и непроменен проектен ъгъл. Причината е самата черта: пробата чете `y(x, z)` от
  ринг-полилинията, а **добавянето на семпъл в ъгъл наистина мени повърхността** — ъгъл, който преди
  се е прерязвал, вече се следва. Тоест пробата не е инвариантна към всичко, а само към плъзгане на
  семпли по прав участък, и това е вписано в самия ѝ текст. Собствената ѝ бележка казва, че падащо
  число не е само по себе си подобрение; сега пише и обратното — покачване иска написана причина.
  Записано и че **отрицателна „загуба" не е бонус, а фасетиране**: главната странична линия чете
  15.9 на меша срещу 6.5 в дизайна, защото мек преход, семплиран на 38 mm, става няколко плоски
  участъка и ъгълът между два от тях може да надмине гладкия завой, който приближават.

- 2026-09-21 — **Разлагане 2×2 на v030: едното твърдение стои, две са грешни, и котвите вършат
  нещо друго от това, за което ги обявих.** v030 смени три неща наведнъж — clip на устната, feature
  anchors и `trans` 72 → 24 на задния undercut — а аз докладвах общия резултат, все едно всяко от тях
  е причината за всичко. Пуснато на една база, с всяко нещо като единствена разлика:

  | конфигурация | устна | undercut | праг |
  |---|---|---|---|
  | обикновен resample, без clip (v029) | 14.2° | — | 12.2° |
  | само clip | **68.0°** | — | 12.2° |
  | само котви | 16.5° | — | **47.7°** |
  | trans 72, обикновен | — | 47.1° | 12.2° |
  | trans 72, котви | — | 36.3° | 50.7° |
  | trans 24, котви (v030) | 66.9° | 44.4° | 47.7° |

  **1. Устната: твърдението стои.** Clip-ът сам дава 68.0° дори с обикновения resample; котвите
  добавят ~2°. Това е дизайнерска промяна и тя работи.
  По пътя дотам **се поправих в движение**: бях премерил „стъпка по y през устната" — 72.5 срещу
  60.6 mm — и оттеглих 67°-те като „предимно представяне". Тази мярка е грешната: тя не различава
  плавна рампа от ъгъл. Пуснах 2×2 вместо да спра на нея и тя ме опроверга.
  **2. Undercut: обосновката ми беше невярна.** Твърдях „2.6–3.4° → 48.8°". Числото 2.6–3.4 е от
  15 септември, върху v017 геометрия, преди `xb` да стане 2950. Сравнил съм септемврийска мярка
  срещу v030 и съм го нарекъл A/B. Истината: при trans 72 и обикновен resample ръбът **вече** дава
  47.1°. `trans` 24 се запазва, но по друга причина — **при включени котви** дава 44.4° срещу 36.3°
  при 72. Седем проби, значи слабо число, и е записано като такова.
  **3. Котвите: вършат нещо, но не това, което обявих.** Не устната и не undercut-а — **прагът**:
  12.2° с обикновен resample срещу 47.7° с котви. Полето му е 34 mm подрез с 10 mm преход, а семпъл
  на всеки ~38 mm по дължина на дъгата не може да покаже 10 mm преход. Котвите **разкриха** ръб,
  който дизайнът винаги е имал; не го създадоха.
  Следствие, което променя и въпроса към собственика: прагът чете 47° **защото вече се вижда**,
  не защото е станал по-остър. Решението в `docs/14` раздел D си остава, но вече се знае какво
  точно се решава.

- 2026-09-21 — **ОТТЕГЛЯМ v032. „Най-голямата единична печалба" не беше печалба изобщо.**
  Смяната на изглаждането по X от „по индекс" на „по най-близка точка" премести кривината
  0.279 → 0.242 и `highlight_test` прочете PASS за първи път. Проверих го с мярка, която **не зависи
  от подредбата на върховете** — самата повърхност като функция `y(x, z)` върху фиксирана мрежа от
  25 mm, където плъзгането на семпли по същата крива не променя нищо. Двете версии са една и съща
  кола: **|d²y/dx²| 17.37 срещу 17.25 mm, |d²y/dz²| 28.17 срещу 28.74.** Оценителят в
  `highlight_test` чете едно-пръстена на върха, а смяната подреди едно-пръстените по посоката на
  повърхността — числото падна, формата не мръдна.
  **Същата грешка я направих два пъти в един преглед:** и „сурова грубост 15.85 срещу 9.27" е
  артефакт — това е втора разлика **по индекс**, а анкерираният resample легитимно слага точките
  другаде. И двете числа, с които обосновах v032, мерят меша, не колата.
  **Четвърти път в този проект число от ДИСКРЕТИЗАЦИЯТА се докладва като факт за ПОВЪРХНОСТТА.**
  Предишните три са записани: удължение на изофоти 34 211:1; „пътуване на границата" 28.0/42.5/60.1 mm;
  и праг, измерен 0.888 m², защото мярката взе огледална двойка за една част.
  Оттеглям и следствията, които обявих: задният undercut 48.8° → 62.5° и прагът 47.4° → 39.7° са
  **същият артефакт** — `edge_test` мери dihedral върху меша. Конфликтът в `docs/14` раздел D **не**
  се е разрешил; прагът е пак 47.4° и решението пак е на собственика.
  Цената на v032 беше и реална: **2 изродени лица** там, където нямаше нито едно, и най-лошият скок
  между нормали на съседни лица 120.6° → 154.6°. Върнато. Бележката с числата остава в кода.

- 2026-09-21 — **`surface_probe.py`: лекарството не е повече внимание, а втора мярка, която същият
  трик не може да помръдне.** Всяко число за формата досега се чете от меша — кривината от
  едно-пръстена, ръбовете като dihedral — значи промяна, която само преподрежда върхове, ги мести.
  Пробата взима `y(x, z)` на мрежа от 25 mm и средната абсолютна втора разлика по двете оси.
  Плъзгане на семпли по същата крива, друг брой точки или друга тесселация не я пипат; огъване на
  повърхността я мени. Вързана е в `check_goal.py` като **контрола**: ако кривината се подобри, а
  `surf_*` не мръдне, повърхността не се е променила.
  Какво НЕ може: казва колко е гладка, не дали е ВЯРНАТА форма — това го казват силуетът, планът и
  ръбовете — и напълно безлична кола дава най-добрия резултат, значи падащо число само по себе си
  не е подобрение. База: **17.372 / 28.166 mm.**

- 2026-09-21 — **v032: изглаждането по X смесваше несвързани части от повърхността, и това беше
  най-голямата единична печалба досега.** Преглед на собствената работа по v030: там твърдях, че
  котвите са фиксиран брой и че ще проверя емпирично за вълни от разместване на индекси — първото е
  вярно (проверено: 10 поискани, 10 запазени, на всичките 91 станции), **второто не го направих.**
  Направено сега: разпределението на семплите по интервали се преизчислява за всяка станция и
  **се мени между съседни станции на 60 от 90 стъпки, с разбъркване до 38 семпъла на една стъпка.**
  Тоест индекс k означава различно място по сечението от ринг на ринг, а `build()` изглажда точките
  **по индекс** — значи смесва несвързани части. Сурова втора разлика по X: 15.85 mm срещу 9.27 при
  обикновения resample; 5.01 срещу 4.26 след изглаждането.
  **Първата ми поправка падна на измерването:** едно споделено разпределение за всички станции дава
  5.73 срещу 5.01 и разбива стъпката до 186 mm, защото интервал, който е дълъг на една станция, е
  прегладнял на друга. Разпределението не е нещото за поправяне — **допускането, че индекс k е една
  и съща черта, е.**
  Затова съседът, който участва в изглаждането, вече е **най-близката точка** на съседния ринг,
  търсена в прозорец ±6 индекса, а не тази със същия номер. Съответствието спира да се предполага.
  Резултат: кривина **0.279 → 0.242**, цилиндрични 47% → 51%, обем непроменен 4.222 m3, заключените
  проверени. `highlight_test` чете **PASS за първи път** и четири от шест зони са „controlled".
  Странични ефекти, и двата в полза: задният undercut 48.8° → **62.5°**, а прагът **47.4° → 39.7°**,
  тоест влиза в G1 лентата и записаното изключение става безпредметно — конфликтът от `docs/14`
  раздел D се разреши сам, без да се пипа нито геометрията, нито лентата. Устната се запази на 65.6°.
  **Което PASS-ът НЕ значи:** осемте най-лоши ленти са още балон и най-лошата се **влоши**
  (врата Z 640–719: 0.79 → 0.86). Изглаждането по най-близка точка пази ръбовете и изравнява
  останалото, тоест събира кривината на по-малко места. Медианата и списъкът с ленти казват различни
  неща и двете са верни — точно затова списъкът съществува.

- 2026-09-21 — **Скрих собствена грешка в /dev/null.** Пуснах пет доклада през zsh цикъл с
  `python3 01_CAD/scripts/$c`, където `$c` съдържаше `panel_registry.py --csv`. **zsh не разделя
  неквотирано разширение на думи**, значи подадох едно име на файл, скриптът не съществува и грешката
  отиде в `2>/dev/null`, който аз бях сложил. `check_reports.py` го хвана — файловете си останаха
  стари — и това е точно за какво е.

- 2026-09-21 — **HAUNCH SAIL: опитано, измерено, отхвърлено — и отрицателният резултат е находката.**
  Хълбокът е най-лошата зона за кривина (0.39 срещу 0.21 на носа), а разбивката по ленти го събира
  на едно място: **Z 800–879 дава 0.61** на 70 върха, а 880–959 — 0.43 на 138. Това е рампата между
  върха на вертикалния фланк (`FLANK_Z_HI` 660) и ръба на deck-а, ~140 mm, в която нищо не държи
  посока — същата форма дефект, който предният калник имаше преди `front_flank()`.
  Лекарството на предницата обаче **не важи тук**: `flank()` тегли сечението НАВЪН към максимума на
  станцията, а навън е грешната посока — ширината е заключена на 925, сечението вече носи 862 при
  Z 800, а референцията прибира тази зона НАВЪТРЕ над рамото към deck-а.
  Затова пробвах лентата да стане **линейчата** — сечението изправено между върха на фланка и
  собствената си корона, което не струва ширина и би трябвало да свали едната главна кривина към
  нула. A/B на една и съща база, силата единствена разлика: 0.0 → зона 0.29 / Z800 0.61 / Z880 0.43;
  0.5 → 0.31 / 0.73 / 0.36; 0.8 → 0.33 / **0.77** / 0.19; 1.0 → 0.28 / 0.70 / 0.31.
  Премества проблема, не го решава: ръбът на deck-а отгоре се оправя рязко, самата лента става
  **по-лоша**, зоната е нула. Изправянето на крива фиксира краищата ѝ и мести средата, а бедата не е
  в средата. Махнато; бележката с числата остава в кода, за да не се опита пак.
  **Изводът е самият отрицателен резултат:** тази лента не е достижима от поле, което действа върху
  цели сечения, а всяко character поле в този файл е такова. `CLAUDE.md` го казва — свободните
  повърхности са ръчна работа на собственика, моята е да проверявам и поправям, не да се правя, че
  скулптирам class-A с примитиви.

- 2026-09-21 — **Устната от v030 струва лентата над себе си, и това не го измерих тогава.**
  A/B с махнат clip на същата база: врата Z 640–719 дава **0.79 с устната срещу 0.54 без**, а
  Z 560–639 — 0.32 срещу 0.20. Медианата по цялото тяло не помръдна (0.29 → 0.28) и скри това,
  точно както казах, че не може да види ръб — но не забелязах, че скрива и съпътстващата щета.
  **Поправка на собственото ми първо заключение:** на по-фини ленти лентата Z 640–760 чете
  0.71–0.78 и БЕЗ устната. Тоест устната добавя ~0.13, но **не я е създала**. Устната остава —
  `docs/16` я иска като G0 и тя мери 67°.

- 2026-09-21 — **„WHERE TO SCULPT" — метриката става адрес.** „MIXED, 0.28" не казва къде да сложиш
  ръка; разбивка по зона × лента от 80 mm казва. Първата ми версия подреждаше само по медиана и
  щеше да сбърка: лента, която е предимно ПЛОСКА, дава само преходните си върхове, а преходен връх е
  сферичен почти по определение. Добавена колона `flat %` и праг — лента с под 45% оформени върхове
  не се подрежда. Тя веднага ме поправи и в другата посока: очаквах да отсее вратата при Z 640–719,
  а тя излезе **0% плоска, 84 оформени върха** — малкият брой, по който бях съдил преди, е просто
  по-рядко семплиране там. Целта стои. Отсече шест други ленти.
  Днешните осем цели: врата 640–719 (0.79), deck 800–879 (0.78), врата 160–239 (0.67), преден
  калник 240–319 (0.64), хълбок 640–719 (0.64), хълбок 240–319 (0.62), калник 320–399 и врата
  240–319 (0.55).

- 2026-09-21 — **`check_reports.py` вече казва КОЙ пише всеки доклад.** „Пусни скриптовете, които
  ги пишат" без да каже кои е точно как трите сирака оцеляха седмица — никой не можеше да разбере,
  че тях не ги пише никой. Картата дава командата и маркира `(B)` за Blender и `(P)` за python3,
  а файл без запис в нея се докладва като **ORPHAN**, което е по-трудно за подминаване от „stale".

- 2026-09-21 — **`check_docs.py`: прозата също не беше проверявана срещу файловата система.**
  Кодът има `check_goal`, `check_reports` и freeze правилото; документите, които ти КАЗВАТ какво да
  пуснеш, нямаха нищо. А точно там излязоха две находки: трите доклада-сираци на 21 септември
  (`CLAUDE.md` сочи скрипт като „изход" на файл, който никой скрипт не пише) и обратното на
  16 септември (`ref-07` и `ref-08` сочени като визуален авторитет, а ги няма в репото).
  Проверява всеки път в обратни апострофи в `CLAUDE.md` и `docs/*.md`. Пет от първите групи находки
  бяха **мои грешки в матчъра**, не в документите — `{persp,top,front}` е скобна експанзия, не glob;
  `docs/16` е съкращение за `docs/16-surface-behaviour.md`; `data/last_build.json` е относителен към
  `01_CAD/scripts/`; `05_MECHANICAL` е име на колекция, не директория; `.blend` в изречението
  „`.blend` файлове" е разширение, не път. Поправени преди доклада.
  `docs/09` се изключва **с причина**: по собственото си правило „не трий стари редове" той записва
  какво е било вярно на дата, значи законно споменава `boundary_sensitivity.py`, който стана
  `donor_exposure.py` на 16 септември. История, която споменава само още съществуващи неща, не е
  история.
  **Истинската находка: `CLAUDE.md` описваше `02_DESIGN/exterior/` като „по един `.blend` на
  семейство панели — `front_clamshell.blend`, `rear_deck.blend`".** Нито един от двата не е
  съществувал никога. Планът се е сменил на v001 и бележката не го е последвала: тялото е ЕДИН
  параметричен строеж, а панелите се извличат от него, значи панел е регион на модела, не собствен
  файл. Това е първото, което чете нова сесия.

- 2026-09-21 — **Докладите в репото могат да остареят, без нищо да забележи, и три от тях бяха
  сираци.** Тръгна от 53 mm: обхватът на прага в `manufacturing_audit.csv` пишеше 292..2122, а
  същите скриптове на същата сцена дават 292..2069. Проверено: комитът на v030 **изобщо не съдържа
  този файл**, а стойността е от **v026** — оцеляла през v027, v028, v029 и v030, докато одитът е
  пускан в тези сесии и обобщението му е докладвано. 53 mm граница на панел във файл, който храни
  пакета за доставчика, четири версии назад.
  Това е същата дупка, затворена на 18 септември за две от входните картинки на `check_goal`, и
  оставена отворена за всеки друг генериран файл. `check_reports.py` я затваря общо, и то **на два
  въпроса, защото едно число не може да отговори на двата**: (1) съответства ли СТРОЕЖЪТ на
  скриптовете — sha на трите build скрипта, подпечатан в `data/last_build.json` от `build()`;
  (2) съответства ли всеки ДОКЛАД на строежа — неговият mtime срещу времето на строежа.
  Първата ми версия мереше само mtime и веднага се опроверга сама: `git checkout` пипна скрипт, без
  да го промени, и целият проект светна червено. Обратното също пада — редактираш скрипт без да
  строиш, и сцената е стара, а всички доклади изглеждат пресни.

- 2026-09-21 — **Три доклада не се пишеха от нито един скрипт.** `donor_conflicts.txt`,
  `scan_dependency.txt` и `panel_register.txt` бяха направени веднъж с ръчно пренасочване на изхода
  на 14 септември и после седяха седмица, изглеждайки като жива информация — а `CLAUDE.md` и
  decision log-ът сочат конкретни скриптове като техен „изход". Доклад, който никой скрипт не може
  да регенерира, е по-лош от никакъв доклад. И трите вече се пишат сами. Още четири се пишеха
  **само зад флаг** (`--csv`), който никой не подаваше.
  При презаписа **четири от седемте смениха съдържание**: `panel_register.txt` твърдеше
  **27 части, RED 11 / YELLOW 16**, докато регистърът е **42 части, RED 17 / YELLOW 25** от
  16 септември; `donor_conflicts.txt` носеше предната полуширина 916 срещу днешните 950 и носа в
  план 350 срещу 500 — числата отпреди поправката на плана във v029. Това са файлове, които вървят
  към доставчик и към външен преглед.

- 2026-09-21 — **Намерено покрай това: `panel_registry.py` цепеше чужд файл по маркер, който не
  съществува в него.** Той взима таблиците на `scan_dependency_report.py`, като exec-ва изходния му
  код, отрязан на `"\ndef main(argv):"` — а този файл има `def main():`, без аргумент. Значи
  split-ът никога не е съвпадал и **целият модул е бил изпълняван**, спиран единствено от неговия
  `__main__` guard. Открих го, като добавих незащитен блок в края на `scan_dependency_report.py` и
  той се изпълни вътре в `panel_registry` и падна на име, което няма. Поправени и двете: маркерът
  става `"\ndef main("`, а блокът е защитен. Код, който тихо прави обратното на това, което пише,
  е една редакция далеч от това да изпълни чужд скрипт вътре в себе си.

- 2026-09-21 — **Деветте фуги не лежаха върху колата. Нито една.** Писани са на ръка на 14 септември
  от референцията и **никога не са сверявани с повърхността**; измерени срещу построената обшивка са
  извън нея средно 25–152 mm, най-зле **210 mm** (`DIFFUSER_to_FASCIA` — три точки на права линия
  при Z 330, минаваща през вътрешността на твърдината, защото линия през колата на една височина и
  една станция изобщо не докосва повърхността, а я пробожда).
  Но грешното е по-тясно от „фугите са грешни": **равнините им са верни и са границите на панелите.**
  `panel_map.B` извежда всяка граница по име от една от тези фуги и числата съвпадат — 440, 1635,
  2000, 3000, 330. Нищо не чете КООРДИНАТИТЕ им; `panel_map` носи свои. Тоест равнините носеха
  тежест, а 3D кривите бяха остаряла декорация, която показва нещо, което го няма — подвеждащо за
  всеки, който отвори модела, за да види къде се делят панелите.
  Затова фугата вече е **само своето намерение**: `station(x, z0, z1)`, `rail(z, x0, x1)`,
  `profile(x, ys, branch)`. Кривата се генерира в `statev_master_volumes.build()`, защото само той
  има `ring()`, а скелетът не може да го внесе (обратната посока). Двайсет и седем ръчно написани
  координати изчезват; няма второ копие на повърхността, с което да се разминат.

- 2026-09-21 — **Генерирането от `ring()` не беше достатъчно, и причината е записана в самия файл.**
  Първият вариант четеше сечението и остави фугите на **9.7 mm средно, 56 при капака на двигателя**.
  Това е точно предупреждението до `built_hw()`: *число, взето от ВХОДА на един лофт, не е факт за
  неговия ИЗХОД.* Ринговете се изглаждат по X през ±2 станции преди лофта и после от резултата се
  режат пет семейства празнини. Затова всяка точка се **снима върху построения меш с лъч** по оста,
  която пази намерението: station и rail пазят X и Z и намират фланка с лъч отвън навътре; profile
  пази X и Y и намира кожата отгоре или отдолу. Всяка страна се снима **върху собствената си
  половина** — огледалното копие оставяше дясната крива до 2.5 mm встрани, защото тялото е
  симетрично по построение, но тесселацията му не е (същата асиметрия, която разминаваше P28/P41
  с 30% на 16 септември). Резултат: **0.00 mm по всички 18 криви, нито една точка не пропусна тялото.**

- 2026-09-21 — **Покритие на картата: 14 от 17 прехода вече имат доказателство** (беше 7 измерими
  линии и дванайсет непроверени). Ключовото разграничение, което промени въпроса: **класът е за
  ПОВЪРХНОСТТА, фугата е за ПАНЕЛИТЕ, и те са независими факти.** На истинска кола G0 чупка много
  често е 4 mm процеп между две допирателни повърхности, а не ръб — значи фуга е доказателство, че
  светлината се чупи, дори когато повърхността не завива. Но фуга **не прави G1 „изпълнен",** защото
  процепът е чупка, не преход, и докладът ги държи в отделни колони вместо да ги смесва.
  Трети вид доказателство: **отвор.** Устата на носа, прорезът на фара и устата на входа са дупки —
  светлината спира там физически, и да напишеш „no line defined" за уста на intake е да докладваш
  липсваща форма, която не липсва. Всеки запис сочи обекта или таблицата, която прави дупката.
  Остават **три без нищо**: `BUTTRESS → REAR_DECK` и `REAR_HAUNCH_TOP → BUTTRESS` — документираното
  BLOCKED семейство, което чака скан S2, защото `DECK_SPINE` стои в обема на сгъване на покрива; и
  `NOSE → HOOD`, който няма дефинирана граница. Той е единствената истинска празнина и не се
  запълва с измислена линия: бележката в `ring()` казва, че носът се управлява от `SECTIONS` S00–S03
  и че местенето им е дизайнерско решение.

- 2026-09-18 — **Донорските CHECK 3 → 1: мярката за арките докладваше ЖЕЛАТЕЛНОТО условие като
  проблем, две сесии подред.** Сравняваше проектирания отвор (`track/2 + open_w/2`) с полуширината на
  тялото, мерена точно извън обхвата на арката, и вдигаше CHECK винаги когато отворът е по-широк —
  26.2 mm отпред, 24.8 отзад. Но **режещият цилиндър на колесна арка ТРЯБВА да излиза извън
  обшивката**, иначе остава мост от материал през отвора. Тоест `open_w` извън обшивката е
  **запас на режещия инструмент**, не грешка, а видимият външен ръб на отвора е собственият силует на
  тялото, никога `open_w`. Старият тест можеше да бъде удовлетворен само като се стесни цилиндърът,
  докато спре да реже — тоест като се създаде истинският дефект, който той твърди, че търси.
  Мярката с следствие е дали арката е отворена, и се мери върху построения меш: има ли останала
  обшивка вътре в цилиндъра, навън от вътрешния ръб на отвора. **Нула върха и на двете.** Проверено и
  директно: при предната ос тялото не носи нищо под Z 600, при задната — нищо под Z 680.
  Остава един CHECK: носът стои +57 mm вътре в собствения навес на донора, а донорското число носи
  ±15–30 mm, тоест 57 е извън лентата и е истински конфликт — иска crash beam геометрия, не мнение.

- 2026-09-18 — **v030: `docs/16`-овата карта на непрекъснатостта стана машинария, а не таблица, която
  нищо не чете.** Измерено на v029 при specX 1200: сечението носи 794° завъртане, а 60-точковият
  resample доставя 524 — **една трета от характера се изхвърля от семплирането**, и най-големият
  ъгъл пада от 86.8° на 46.4°. От останалото само две места са ръбове (подът на Z 120 и прагът на
  Z 315); каналът на вратата, горната му устна и рамото над него са рула от 4–13° на стъпка,
  разлени на 70–90 mm. `docs/16` иска **G0** на `DOOR_UPPER → DOOR_CHANNEL`. 12° не е G0.
  Бележката в `VOID_FIELDS` от 15 септември беше поставила диагнозата вярно — *"a G0 crease here
  needs explicit control points … at 60 points per half-section the resample cannot resolve a
  transition that narrow"* — и я беше отложила за „повърхност, която още не съществува".
  Повърхността съществува. Три промени: (1) `resample_anchored` + `feature_anchors` — всяка станция
  закотвя sample точно върху десет наименувани надлъжни линии, **фиксиран брой**, всяка от таблица
  непрекъсната по X, за да не изчезва котва между съседни станции (това счупи вътрешния ръб на
  buttress-а на 15 септември); (2) устната на канала се **затваря** за `LIP_TRANS` 18 mm вместо да
  избледнява — нищо не се ДОБАВЯ, тоест заключената 1850 не може да се пипне; (3) `trans` на задния
  undercut 72 → 24, защото вече е разрешим. Резултат: устната **12° → 67°** (44–72 на 14 станции от
  specX 510 до 2040), задният undercut **2.6–3.4° → 48.8°**. Един ръб за цялата страна, не по един
  на поле: при specX 2107 двете устни стояха на 14 mm една от друга и ъгълът падаше на 0 точно
  където страничната линия влиза в устата.

- 2026-09-18 — **`edge_test.py`: единственото измерване, което този проект никога не е имал.**
  Три числа описваха формата (силует, план, кривина) и **нито едно не може да види ръб**: първите
  две сравняват контури, а `highlight_test.py` взима МЕДИАНА по ~6000 върха, а ръбът е две редици
  от шейсет — затварянето на устната вдигна ъгъла от 12° на 74° и премести медианата с 0.01.
  Собствената му бележка го признава: *"it cannot see whether the light BREAKS at the three voids."*
  Тоест характерът на колата — който Е нейните ръбове — беше неизмерен, докато три числа казваха,
  че формата е вярна. Тестът мери **двустранно**: G0 иска 40–110°, G1 8–40°, G2 под 9°. „По-остро е
  по-добре" е как колата става фасетирана; G2 линия на 40° пада толкова шумно, колкото G0 на 4°.
  Мери се върху **построения** меш след булевите, не върху `ring()` — ринговете се изглаждат по X
  преди лофта и измерване върху входа не вижда какво прави това. Резултат: **6 в лента,
  1 записано изключение, 0 дефекта** от 7 измерими линии. 12 прехода от `CONTINUITY` остават
  неизмерими, защото не съществува таблица с височина за тях, и са изброени по име.

- 2026-09-18 — **Две мои собствени грешки в този тест, намерени и поправени преди да се докладват
  като дефекти в колата.** (1) `REAR_HAUNCH_TOP → REAR_HAUNCH_SIDE` излезе с медиана 90° и максимум
  124° на пет проби; причината не е хълбокът — `FLANK_Z_HI` е 660, а задният срез на арката,
  цилиндър с радиус 350 на оста, коронясва на същата височина, тоест мерех **вертикалната стена на
  отвора на колелото**. (2) Очевидният филтър — „и двете лица да носят Y в нормалата" — е **грешен**:
  при гребена на предния калник повърхността се превръща през върха, и двете лица гледат нагоре, и
  той изхвърли седем от девет проби, после обяви медиана 7.3° от двете останали и каза, че гребенът
  е твърде мек. Медиана от две проби не е измерване. Верният тест е **радиален** — обшивката гледа
  навън от собствената ос на тялото, а стената на срез гледа по среза. Добавена и преграда: под
  4 проби не се дава присъда. (3) И трета: бях класифицирал `FRONT_CREST` като
  `HOOD → FRONT_FENDER_TOP` (G2) и обявих 38.6° за дефект. Той е външната горна линия на калника,
  тоест `FRONT_FENDER_TOP → FRONT_FENDER_SIDE` (G1), и 38.6 е в горния край на лентата.

- 2026-09-18 — **Прагът е записано изключение, не дефект и не мълчаливо разширена лента.**
  `DOOR_UPPER → ROCKER` мери ~47° при G1 лента 8–40. Прагът беше **умишлено** изострен 55 → 18 →
  10 mm на 15–16 септември, защото четеше като меко хлътване без линия. `docs/16` дава КЛАСА, не
  числото — лентата 8–40 е моя интерпретация. Затова не мърда нито геометрията, нито лентата:
  изключението се записва с причината в `EXCEPTIONS`, по същия модел като freeze правилото, и НОВА
  линия извън лента пада шумно. **Решението е на собственика**: или лентата е твърде тясна за праг,
  или преходът се връща на ~18 mm.

- 2026-09-18 — **`ortho_views.py`: липсващата стъпка беше цял процес, не файл.** `model_side.png` и
  `model_plan.png` са ВХОДОВЕ на две от трите измервания на целта, и до днес не бяха произвеждани от
  нито един скрипт — ad-hoc код в сесията, която ги е поискала. Стар PNG не пада: сравнява СТАРАТА
  геометрия с референцията и печата „nothing regressed". Първата версия на файла рендърираше с
  авто-кадриране и `silhouette_overlay` върна **441 mm** срещу 15 — не заради геометрията, а защото
  `model_side.png` има **твърд контракт**, закодиран в началото на `silhouette_overlay.py`: точно
  1680×560 px, 2.8753 mm/px, предна ос на земята в пиксел (417.4, 521.7). Затова сега **не
  рендърира**: маската е измерване, а рендърът слага engine, материали, светлини, world, film и
  colour management между меша и числото. Растеризира се от върховете, а контрактът се assert-ва.
  Проверено: v029 през новия път дава **точно записаната база 15 / 3 / 21 / 2.3 / 0.29**, тоест
  старият рендър е бил верен и новият път го възпроизвежда до число. `check_goal.py` вече проверява
  възрастта на четири входа, не на един, и носи пета метрика `edge_defects` с допуск 0.

- 2026-09-18 — **Донорските CHECK 4 → 3: мярката за задното крило беше попаднала в отвора.**
  „Air over the welded quarter" върна our skin 579 срещу donor 883 при specX 2116 и обяви −304 mm,
  докато на 16 септември същата мярка даде 883 срещу 882. Разликата: stage 03 реже истинска уста на
  входа при specX 1955–2165, Z 448–700, навътре до Y 430, тоест в тази лента най-широката повърхност
  е **подът на джоба**. Скриптът вече изключваше отвора на кабината по същата причина; сега
  изключва и устата. Вярното число е **+34 mm въздух**, присъда `SCAN`, защото разликата е под
  собствената ±30 mm грешка на донорския план. **Пети път в този проект измерване намира дупка и я
  докладва като панел.**

- 2026-09-18 — **Нула секции извън плота и нула файлове с по две парчета — за първи път.**
  Две отделни находки. (1) `plan_cuts` изчисляваше клетката като плот − 2×поле − езиче, но всяка
  секция след това получава стена от `thicken()`, която избутва bbox-а с до една стена на страна:
  310 + 20 + 4 = 334 при 330 използваеми, и `P21` секция 16 излезе `FITS_BED=NO` — скриптът твърди
  инвариант, за който не си е запазил място. Същото семейство като двете парчета на дифузьора, които
  се изнасяха 2.9 mm над плота. Клетката вече вади и `2 × wall`. (2) Острият undercut ръб накара
  черупката на задната маска да се сгъва, и 3D решетката хваща две отделни части от нея в една
  клетка: `P21` отиде от 6 на 34 секции по две парчета. Верният извод не е да омекотя ръба, а че
  **файл с две несвързани твърдини не е част, и да го наречеш изключение не го прави част** —
  разделят се на по един файл. Резултат: **273 от 273 секции едно парче, 0 извън плота.**

- 2026-09-15 — **Находка: `HOOD_SPINE` не управлява носа.** През носа `ring()` взима
  `max(crown - drop, z_top + 10)`, и сечението бие spine-а на четири от шест станции между
  specX −950 и −450. `SECTIONS` S00–S03 носят горни точки на Z 450, 550, 650 и 700 — стълба, която
  надделява каквото и да иска `HOOD_SPINE`. Той става авторитетен едва от около specX −250 назад.
  Проверено: три различни таблици за `HOOD_SPINE` дадоха еднакъв нос до 4 mm.
  Следствие: `docs/16` иска носът да е „надлъжно почти плосък" и изрично „не вдигай центъра", а той
  се качва със 100 mm на първите 200. Лостът е в `statev_skeleton.py`, не в
  `statev_master_volumes.py`, и местенето му мени силуета на носа, който CHECKPOINT 01 гледа —
  тоест **дизайнерско решение, не поправка**. Не е направено. Чака одобрение.

- 2026-09-15 — **`check_packaging.py`: всичките 31 обвивни кутии срещу построеното тяло.**
  Обобщение на проверката за лампите, защото моделът вече струва четири пъти: **число, взето от
  ВХОДА на един лофт, не е факт за неговия ИЗХОД.** За всяка кутия се мери колко стърчи над тялото
  и колко навън от полуширината, на нейната собствена станция и в нейната собствена Y лента.
  Критерият зависи от колекцията: `04_LIGHTING` и `05_MECHANICAL` са кухини и всяко стърчене е
  дефект; `03_AERO` са повърхностни елементи с допуск 25 mm; `02_BODY` са самото тяло и се
  докладват само информативно.
  Намери две. **`REAR_SPOILER` стоеше 69.6 mm над деката** — тоест крило на невидими стойки, точно
  обратното на това, което собствената му бележка иска („integrated lip / ducktail, NOT a fixed
  wing on stalks"). Свален от Z 770 на 720; вече стои 19.6 mm над повърхността и израства от нея.
  **`BUTTRESS` стои 93.3 mm над тялото** — това е известният блокаж, не нов дефект: кутията носи
  намерението от рендера (острие до Z 1090), а построеното е 10 mm устна. Вписан в `KNOWN_BLOCKED`,
  за да си остане видим, без да вдига фалшива тревога всеки пуск. Проверка, която вика вълк, спира
  да се чете.

- 2026-09-15 — **Стоповете се поставят спрямо построеното тяло, а не опашката се вдига заради
  тях.** Опашката мина CHECKPOINT 01 и короната ѝ не се мени заради графика.
  `TAIL_BAR` беше specX 3230, Z 700, ширина 1560, на основанието че „Z 700 стъпва на рамото S13,
  чиято горна контролна точка е 750" — пак срещу контролните данни на сечението, не срещу тялото,
  което те произвеждат. Построеното тяло там е високо 642.6 и широко 1498, тоест лентата **стърчеше
  76 mm и надвисваше по 31 mm от всяка страна**. Сега: specX 3200, Z 612 (горен ръб 631, 12 mm под
  короната), ширина 1440. Намерението не е пипано — тънка, през цялата ширина, високо на опашката.
  `TAIL_END` беше specX 3150, Y 790, Z 700 и стоеше 283 mm над тялото, защото при тази станция
  четвъртината вече е паднала навън от Y 770. Сега specX 3120, Y 700, Z 612 — на същата височина
  като лентата, за да чете L-ът като една непрекъсната светлина.
  `check_lighting.py` получи и **проверка по ширина**: лампа не може да е по-широка от колата на
  собствената си станция. Тя е това, което липсваше — гледаше се само височина.
  И четирите лампи вече минават: 0 проблема.

- 2026-09-15 — **Фарът е преместен от specX −700 на −600, защото стърчеше 41.5 mm през
  каросерията.** Мерено върху **построената** повърхност, в собствената Y лента на модула
  (505–615), тялото при −700 стига до Z 583.5, а кухината иска 625. Бележката в `BOXES` твърдеше
  полуширина 680 при S02 Z 570; построеното дава 652 там, защото проверката е била правена срещу
  контролните данни на сечението, а не срещу повърхността, която те произвеждат. Същият клас
  грешка като с cutter-ите във v016. Модулът не може да слезе — светещият му долен ръб е на 515 при
  законов минимум 500, тоест 15 mm запас. Първата станция, на която влиза, е −600 (652.2).
- 2026-09-15 — **`check_lighting.py`**: проверява всяка лампа срещу построеното тяло, не срещу
  данните на сечението. Две проверки — дали кухината е наистина вкопана и дали светещият ръб е над
  500 mm. Веднага намери още два конфликта, които стояха незабелязани: **`TAIL_BAR` стърчи 76 mm**
  над задното тяло при specX 3230, а **`TAIL_END` — 283 mm**. И двата идват от височини, свалени от
  рендера, докато нашата опашка е по-ниска от неговата. Не са поправени: или лампите слизат, или
  опашката се вдига, и това е дизайнерско решение. Зоната LIGHTS е извън обхвата на CHECKPOINT 01.

- 2026-09-17 (v029) — **Планът е поправен: 5.9% → 2.3%. И първо трябваше да оттегля собствения си
  резултат, защото го бях нормализирал по ОГЛЕДАЛАТА.**
  Първата таблица даваше спайк 1.000 при 39% от дължината, самотен между 0.862 и 0.849. Форма на кола
  не прави такова нещо. Проверен на фини стъпки: 37–41%, пет станции, издига се 137 → 159 px и пада
  обратно на 135 — дискретен предмет, дълъг ~220 mm и стърчащ ~140 mm. **Това са вратните огледала**,
  а нашата кола няма такива: `P37/P38` е BLOCKED, защото скелетът няма кутия за огледало.
  Делил съм всяка станция на число, **18% по-голямо от истинския максимум на тялото**. Оттам и
  „средата е твърде пълна" — наполовина артефакт. Лентата 36–42% вече се изключва от нормализацията
  **по станция, не по праг** — праг би изял и истински широк ханш.
  **Вярното, след поправката, и трите поправени:**
  - **Нос: −142 mm полуширина на върха.** `S00/S01/S02` разширени в план. → **+5 mm**
  - **Опашка: +375 mm.** Рендерът пада 0.797 при 96% до 0.131 при 100% — последните му **175 mm са
    тесен централен елемент над дифузьора**, не каросерия. Ние държахме 0.536 и свършвахме с плоча.
    `S14` стеснен, плюс **нова станция `S13b` на 3320**, защото първият опит започна стеснението
    твърде рано и преборщи 96% с 84 mm. → **+46 mm**
  - **Талия в средата: +57…+73 mm** през specX 930–1540. Ново поле `WAIST` — **само в план, без Z
    член**, за да не мръдне силует, който вече съвпада. → **−1 до +42 mm**
  **Резултат: среден план 5.9% → 3.5% → 2.3% от максималната полуширина, тоест 22 mm на кола, широка
  1850 — 1.2%.** Най-зле 46 mm.
  **Силуетът е непокътнат** — 3 mm отпред, 15 mm средно, проверено след всяка стъпка. Точно за това
  полетата нямат Z член.
  Заключените непокътнати: 4370 / 1850 / 2415. Осветление 0, опаковане 0, карта unassigned 0,
  одит 21/2/13/6, кривина 0.29. **20 пилотни STL, 163 секции, 149/163 едно парче, 184 файла,
  115 364 триъгълника, 0 извън плота.**
  **`check_goal.py` вече пази и трите числа** — силует, план, кривина. База: front 3.00, rear 21.00,
  mean 15.00, **plan 2.30**, curvature 0.29.
  Файл `STATEV_001_v029.blend`, хеш `fdc415a551c78950`. Откат: v028.

- 2026-09-17 — **`ref-09` пристигна. Планът е мерен за първи път, и той казва конкретно какво е
  различното.**
  Комплектът е четири изгледа плюс три детайла, една кола. **Страничният е по-ортогонален от
  `ref-05`** — двете главини се разминават с 0.3 px = **2 mm** (срещу 6). Калибровка **5.2685 mm/px**.
  **Проверката за съгласуваност мина ПЪРВА и отсече два изгледа.** Страничният се калибрира по
  собственото си междуосие → дава дължината; дължината калибрира изгледа отгоре; височината
  калибрира предния и задния. Трите тогава трябва да се съгласят за една ширина:
  **отгоре 1890, отпред 2019, отзад 2205 — разлика 315 mm, 15.5%.** Причината се вижда в самите
  кадри: в предния и задния се вижда вътрешността на кабината, тоест камерата е **над** колата и
  височините са скъсени. **Ползват се само страничният и изгледът отгоре.** Предният и задният
  остават референция за окото, не измерване. (Отгоре дава 1890 срещу нашите заключени 1850 — 40 mm,
  тоест ширината е наред.)
  **Колелата не се виждат отгоре**, значи изгледът не може да се калибрира по междуосието. Не е
  нужно: сравнението е **нормализирано** — всяка станция е дял от собствената дължина на колата,
  всяка полуширина е дял от собствения ѝ максимум. Две коли с различни размери и еднакъв план дават
  еднакви криви. Калибровката става без значение и не може да се сгреши.
  **Резултат — средно 8.0% от максималната полуширина, тоест ~74 mm на нашата кола. По ред:**
  1. **Носът е твърде остър в план.** През първите 8% от дължината рендерът е по-широк от нас
     постоянно: **+0.133 на самия връх** (123 mm полуширина, тоест ~240 mm обща ширина),
     +0.099, +0.024, изравняват се към 12%.
  2. **Средата е твърде пълна.** През 43–57% сме по-широки с +0.095 до **+0.112** (88–104 mm
     полуширина). Рендерът има талия там, където ние сме прави.
  3. **Опашката е твърде тъпа.** Рендерът пада 0.491 → **0.126** в последния 1%; ние стоим на
     **0.536**. Стеснява се до връх; ние свършваме с плоча. Физически това е последните ~40 mm, но
     е ясен подпис на формата.
  **Заедно:** планът на рендера е **тъп нос, талия в средата, връх отзад.** Нашият е **остър нос,
  пълна среда, тъпа опашка.** Това е разликата, която прави колата „да няма нищо общо", докато всеки
  размер излиза верен — и до днес не е била измервана, защото всяко измерване беше в една проекция.
  Нов `plan_overlay.py`. `ref-09` е записан като **текуща цел** в README на референциите, с изрична
  бележка кои два изгледа са измерими и кои не.

- 2026-09-17 (v028) — **Референцията влезе в сцената, на верен мащаб, и е ПРОВЕРЕНА.**
  `ref-05` е поставена като image empty `REF05_SIDE` в нова колекция `REF_TARGET`, на калибровката,
  която наслагването вече установи — 2.8753 mm/px, собственият ѝ произход (px 393, 491.1 = предна ос
  на земята) легнал върху произхода на колата.
  **Проверено числено, не прието:** пиксел (393, 491.1) → specX **0.0**, Z **−0.0**; пиксел
  (1232.9, 377.2), задната главина → specX **2415.0**, тоест точно междуосието. Центърът на предното
  колело излиза Z 333.8 срещу нашите 323.5 — **10.3 mm**, което е в записаната лента: рендерът си
  има собствена гума, малко по-голяма от нашата 235/35R19, и двете оценки за земята вече се
  разминаваха с 20 mm.
  Геометрията е **идентична на v027** — v028 добавя само референцията във viewport-а.
  *(Image empty не се рендира — вижда се само във viewport-а. Затова проверката е числена, а не през
  рендър; два опита да я потвърдя с кадър не показаха нищо и са изтрити.)*
- 2026-09-17 — **Отхвърлих две свои предложения, и това е същественото решение.**
  Предложих Hyper3D/Hunyuan да генерира 3D подложка от `ref-05`. Критично: **такъв меш измисля
  каквото не вижда** — в проект, чиято цялата дисциплина е „не измисляй донорски размери". Щеше да
  даде 3D нещо, което изглежда като напредък и не може да се провери срещу нищо. Остават изключени.
  Отхвърлих и втория си план — калибриране на двата 3/4 изгледа — по друга причина: собственикът каза
  **„размерите са добре, формите нямат нищо общо"**. Да меря повече размери не адресира това.
  **Третият отговор на „трябват ли рендъри на всеки панел" е НЕ, и причината е по-силна от първите
  две:** всяка генерация измисля; дванайсет рендъра на панели са дванайсет различни коли; и **панел
  сам по себе си не дава на калибровката за какво да се хване** — един калник няма междуосие в себе
  си.
  **Това, което би помогнало, е комплект от четири ортогонални изгледа на СЪЩАТА кола** — страна,
  отпред, отзад, отгоре. Във всеки има колела, а следата 1465/1528 и междуосието 2415 са публикувани,
  тоест всеки изглед носи собствената си калибровка — точно както страничният я носи. С тях същата
  машинария мери формата **в трите оси**. Планът е най-голямата сляпа зона: страничният изглед не
  казва нищо за него.

- 2026-09-17 (v027) — **Пуснах три версии, без да меря целта. Ето какво беше счупено и как го
  затварям с код.**
  v024, v025 и v026 минаха само на счетоводните проверки — карта, заключени, опаковане, осветление.
  **Наслагването срещу рендера и тестът с кривината не бяха пускани нито веднъж.** Точно грешката,
  която собственикът посочи: меря лесното, не същественото.
  Пуснати сега, върху построеното: **предницата беше регресирала 3 mm → 21 mm, най-зле 81.**
  Причина: **лайсната на канала в калника стърчеше 75–109 mm над каросерията.** Написана беше с
  фиксиран връх Z 974, а короната на тялото там е 865–899 — тоест перка, стърчаща от капака, която
  наслагването отчиташе като горна линия на колата. Лайсна, която стърчи от панела, който облицова,
  не е лайсна.
  И втора, по-скъпа: **прорезът беше яхнал гребена на калника.** При Y 470–660 той стъпва върху
  гребена, който седи на Y 590, и изяждаше **18 mm от короната** — точно линията, заради която е
  добавено полето `FRONT_CREST` във v021. Преместен на **Y 385–545**, вътре от короната, върху
  вътрешното рамо — където един вентилационен прорез на калник и принадлежи.
  След двете поправки: **предница 3 mm, средно 15 mm** — върнато.
  **Но истинският дефект не беше геометрията, а процесът**, затова е затворен с код:
  **`check_goal.py`** пуска двете измервания, които **са** целта — силуетът срещу `ref-05` и
  отношението на главните кривини по `docs/16` — сравнява ги със записана база и казва **REGRESSED**
  на глас. Кривината се мери в Blender, затова `highlight_test.py` я записва във файл, а
  `check_goal` **проверява възрастта му**: стойност, по-стара от последната промяна по строящите
  скриптове, е стойност за друга кола и не се докладва.
  База, записана върху v027: **front 3.00, rear 21.00, mean 15.00, curvature 0.30.**
  Останалото непроменено: части 36, одит **21 PROCEED / 2 / 13 / 6**, обшивка 15.60 m², 20 пилотни
  STL, **157 секции, 143/157 едно парче, 0 извън плота**. Заключените непокътнати.
  Файл `STATEV_001_v027.blend`. Откат: v026.

- 2026-09-17 (v026) — **Корпусите и каналите построени. BLOCKED 19 → 6.**
  - **Корпус на фара `P24/P25`** — черупка 5 mm около `PROJECTOR` обвивката (DECIDED, specX −600,
    Y ±560, Z 570, 150×110×110 за един Hella 90 mm bi-LED), отворена отпред, където стои рамката
    `P29`. Модулът **никога не се пипа** — това е записано решение и черупката е построена около
    него с въздух, не върху него.
  - **Канал на входа `P31/P32`** — loft през трите обвивки, които скелетът вече носи:
    `INTAKE_INLET` (derived), `INTAKE_DUCT` и `INTAKE_OUTLET` (и двете **PROVISIONAL**). Формата е
    наша, **маршрутът не е** — къде реално седи плениумът е донорски отговор, затова частта пада в
    `SCAN REQUIRED`, не в PROCEED, и това е правилното място за нея.
  - **`P37/P38 MIRROR_CAP` НЕ е построена, и причината е смисълът на упражнението:** в целия
    `statev_skeleton` **няма кутия за огледало** — нито обвивка, нито позиция, нито размер. Да
    построя капак значи да измисля къде е донорското огледало. Остава BLOCKED до скана.
  **Резултат:** части с геометрия **32 → 36**; одит **19 → 21 PROCEED**, `SCAN REQUIRED` 11 → 13
  (каналите, правилно), **BLOCKED 10 → 6**; обшивка 13.99 → **15.72 m²**; ядро 58.5 kg.
  **20 пилотни STL** и **157 печатни секции, 143 от 157 едно парче, 178 STL файла,
  104 620 триъгълника, 0 извън плота**. За котиране: **20 части, 5.75 m², ~21.4 kg**.
  Заключените непокътнати — 4370 / 1850 / 2415. Осветление 0, опаковане 0, карта unassigned 0.
  Файл `STATEV_001_v026.blend`. Откат: v025.
  **Шестте останали BLOCKED са всички зад покрива:** `P23` спойлер, `P33` ламели, `P26/P27` корпуси
  на стоповете, `P37/P38` капаци на огледала. Първите четири чакат deck-а, който е `ROOF_BLOCKED`;
  последните две чакат обвивка, която не съществува. **Нито един от тях не е блокиран от нещо, което
  мога да построя.**

- 2026-09-17 (v025) — **Задницата и фаровете отворени. BLOCKED 19 → 10.**
  Продължение на същия механизъм: джоб с остър ръб, и част, стояща в него.
  - **Задна централна маска `P34`** — джоб specX 3330–3425, |Y| < 300, Z 395–595, с рамка 26 mm в
    него. В `ref-05` опашката не е стена: вдлъбната централна маска седи между двата края на
    светлинната лента.
  - **Гнездо за номер `P36`** — джоб specX 3345–3425, |Y| < 255, Z 250–372, рамка 20 mm.
  - **Обрамчение на ауспуха `P35`** — **една част, не две.** Регистърът го води като една част, така
    че е построено като една греда с два изрязани отвора при Y ±65, Z 430 — там, където `EXHAUST`
    кутията вече стои. Два отделни пръстена биха били една част в две парчета, което преградата за
    свързаност в `pilot_panel` щеше да откаже — и с право.
  - **Фарът като острие, не кухина `P29/P30`** — прорез през носа specX −700..−540, Z 505–625, с
    рамка 18 mm в него. Това значи „thin light blades" и е единственият законен начин да се получи:
    E-маркираният модул стои зад рамка и **никога не се пипа**.
  **Нищо не е близо до покрива.** `P17, P18, P19, P42, P20, P23, P33` са `ROOF_BLOCKED` и не са
  докоснати; предполагаемият обем на сгъване е specX 1240–1760, а всичко тук е зад 3300 или пред
  −540.
  **Резултат:** части с геометрия **27 → 32**; одит **14 → 19 PROCEED**, **BLOCKED 15 → 10**;
  обшивка 13.05 → **13.99 m²**; ядро 52.0 kg. Изнесени **18 пилотни STL** (пак без `P02`) и
  **147 печатни секции, 135 от 147 едно парче, 166 STL файла, 103 116 триъгълника, 0 извън плота**.
  За котиране: **18 части, 5.40 m², ~20.1 kg**.
  Заключените проверено непокътнати — 4370 / 1850 / 2415. Осветление 0, опаковане 0, карта
  unassigned 0. Файл `STATEV_001_v025.blend`. Откат: v024.
  **Остават 10 BLOCKED**, и те се делят на две: `P23, P33` и корпусите на стоповете чакат deck-а,
  който е `ROOF_BLOCKED`; `P24/P25` корпуси на фаровете, `P31/P32` канали на входа, `P37/P38`
  капаци на огледала — те са следващите, които могат да се построят.

- 2026-09-17 (v024) — **ЕТАП 03 започна: първите наслоени елементи и първите истински отвори.**
  Нов `stage03_elements.py`. Loft-ът остава **ядрото** — пропорция, опаковане, панелна карта стъпват
  върху него. Отгоре се строят елементите, които loft не може да направи.
  **Отворите се режат като джобове с вертикални стени и остър ръб**, не като вдлъбнатини. Това е и
  каквото рендерът показва, и какво е един истински вход — входът храни канал, не се гледа през него.
  **Остриетата са отделни обекти с въздух зад тях**, защото този въздух е целият смисъл на
  наслояването.
  Построени, и всяко от тях **вече беше в регистъра като BLOCKED „няма геометрия"**:
  - **страничен вход** — джоб specX 1955–2165, Z 448–700, рязан от повърхността на Y 901 навътре до
    430; плюс **острие 22 mm** в устата → **`P13/P14 INTAKE_BLADE`**
  - **прорез в предния калник** — specX −235..165, Y 470–660, рязан отгоре надолу от Z 985 до 700
    (вентилира арката, както `AIRFLOW` казва); плюс **лайсна 14 mm** в прореза →
    **`P05/P06 FENDER_CHANNEL`**
  **Всичко е панел по панел, както собственикът настоя.** Елементите носят своя `panel_id`, минават
  през `panel_extract`, одита, пилотната верига и производството като части, не като декорация.
  Картата научи новите джобове — стената на джоба гледа в канал, не към пътя, затова се отхвърля
  като `X_INTAKE` (0.512 m²) и `X_FENDER_SLOT` (0.449 m²), а картата **пак затваря с unassigned 0**.
  **Следствия:** части с геометрия **23 → 27**; одит **10 → 14 PROCEED**, BLOCKED **19 → 15**;
  обшивка 12.35 → **13.05 m²**; ядро 48.6 kg. Изнесени: **13 пилотни STL** (P02 не влиза — неговият
  PROCEED е на артефакт) и **130 печатни секции, 121 от 130 едно парче, 144 STL файла,
  100 768 триъгълника, 0 извън плота**. За котиране: **13 части, 4.71 m², ~17.5 kg**.
  **Заключените са проверено непокътнати** — 4370 / 1850 / 2415. Осветление 0, опаковане 0.
  Записано и че `P03/P04` стават 4 парчета, а `P11/P12` също 4 — прорезът и устата ги режат. И двете
  са CONDITIONAL/SCAN REQUIRED, тоест без STL, но са въпрос към регистъра.
  **Това е първата стъпка от 19, не завършекът.** Остават: обрамчения и корпуси на фарове, корпуси
  на стопове, ламели на капака, задна централна маска, обрамчение на ауспуха, гнездо за номер,
  спойлер, капаци на огледала — и отварянето на задницата, което е това, което ще промени четенето
  най-много след входа.

- 2026-09-17 — **Собственикът: „формите нямат нищо общо с рендера". Прав е, и ето точната причина.**
  Проверих две неща, преди да отговоря. Първо — дали е само засенчване: геометрията **има** 2047
  ръба над 30°, но всичко беше shade smooth, което усреднява нормалите и заобля всеки ръб. Пуснат
  auto smooth на 28°. Стана малко по-остро и **това не беше проблемът**.
  Второто е истинското и е структурно: **тялото е ЕДИН затворен lofted обем** от 15 сечения плюс
  скаларни полета. Рендерът е **наслоени панели с въздух между тях** — точно както `CLAUDE.md`
  описва посоката: „layered panels with negative space, floating fenders, thin light blades, aero
  fins behind the roll hoops, open rear structure over diffuser".
  Един loft **не може** да произведе панел, стърчащ пред друг с процеп зад него; тънко острие;
  отвор, през който се вижда; ръб, който остава остър по цялата си дължина. Това, което имаме, са
  **полета за дълбочина** — вдлъбнатини в непрекъсната кожа. Страничният вход е вдлъбнатина.
  Каналът на вратата е вдлъбнатина. „Buttress"-ът е издутина върху deck-а. Затова се чете като един
  сапун с трапчинки — защото **е** един сапун с трапчинки.
  **И моите доклади бяха честни по числа, но подвеждащи по смисъл.** Мерех **контура** и докладвах
  3 mm, и оставих това да мине за „близо до рендера". Контурът никога не е бил нещото, което прави
  колата да прилича на рендера.
  **Регистърът вече го знае:** **19 части са BLOCKED „няма още геометрия — Stage 03 детайл"** —
  каналите на калниците, остриетата и каналите на входа, обрамченията и корпусите на фаровете,
  корпусите на стоповете, ламелите, задната централна маска, обрамчението на ауспуха, гнездото на
  номера, спойлерът, капаците на огледалата. **Това са формата.** Без тях не може да прилича, каквото
  и да правя с loft-а.
  Проверено и още едно: `DONOR_INTERFACE` на тези 19 говори за **захващане**, не за форма. Формата на
  канал в калника, острие във входа, обрамчение на фар, спойлер, гнездо за номер — тя е **наша**,
  върху **наша** повърхност. Сканът дава точките на захващане, а собственикът изрично каза те да се
  мислят после.
  **Решението:** спирам да третирам loft-а като колата. Той е **ядрото** — пропорция, опаковане,
  панелна карта стъпват върху него и остават. Наслоените елементи се строят **отделно**, с истински
  процепи и истински отвори. Това е работата, която прави разликата, и тя не чака скана.

- 2026-09-17 (v023) — **Балонът е локализиран и намален там, където беше най-лош.**
  Разбивката по X зона × Z лента показа точно къде стои: **предният калник е лош на всяка лента** —
  0.47 / 0.49 / 0.40 / **0.54** / 0.23 — а най-зле е Z 660–800, рамото между фланка и новия гребен.
  За сравнение задното рамо дава 0.36 / 0.30 / **0.22** / 0.47 / 0.37, а каналът на вратата слиза до
  **0.12**. Причината е ясна от самия код: **задницата получи `flank()` на 14 септември, предницата
  никога.** Всяко предно сечение е гладка дъга от пода до короната — точно това, което коментарът в
  `statev_master_volumes` описва като „рамото четеше като издуване" за задницата.
  Добавено е поле **`front_flank`** — същият механизъм, specX −700..500, Z 480..800, pull 0.82.
  Държано е като **отделна функция**, а не втора лента вътре в `flank()`, защото `FLANK_SHOULDER` и
  бележката за канала на вратата стъпват върху константите на задната лента и сливането би променило
  задницата, докато оправя предницата.
  **Резултат:** зоната на предния калник **0.42 → 0.27**, наравно с носа и опашката. По ленти:
  0.47→0.29, 0.49→0.46, 0.40→0.35, **0.54→0.32**, 0.23→0.16. Цяло тяло **0.31 → 0.29**, цилиндрични
  **43% → 46%**.
  **Опитано и отхвърлено:** сваляне на долния край на лентата 480 → 380 прави лентата под нея
  **по-лоша** (0.46 → 0.56), защото долната рампа тогава пада вътре в Z 330–500, а рампата сама по
  себе си е кривина в две посоки. Оставено на 480, където рампата пада в лента, която вече се държи.
  **Страничен ефект, който затваря донорски CHECK:** разширението от фланка вдига предната
  каросерия при короната на арката и **предната гума вече не стърчи** — беше −5.1 mm, сега **+7.5**.
  Донорските CHECK-ове стават **4 → 3**.
  **Нищо друго не е регресирало:** силуетът срещу рендера непроменен (средно 15 mm, предница 3 mm),
  заключените 4370 / 1850 / 2415 непокътнати, осветление 0, опаковане 0, карта unassigned 0.
  Одит **10 / 2 / 11 / 19**. Обшивка 12.13 → **12.35 m²**, ядро 45.9 kg.
  Панели презчислени: **124 секции, 117 от 124 едно парче, 134 STL файла, 100 128 триъгълника,
  0 извън плота.** Център на тежестта specX 1310 (54% назад), Y −3.4 mm.
  Файл `STATEV_001_v023.blend`, хеш `e04f2a8fbe648661`. Откат: v022.
  **Най-лоша остава зоната „рамо и intake" на 0.36** — нейният проблем е Z 660–800 (0.47), където
  `FLANK_SHOULDER` слага гаусов връх, а гаусът е сферичен по природа. Не е пипана съзнателно:
  `P15/P16` са SCAN REQUIRED overlay върху завареното крило и формата им се преправя при скана.

- 2026-09-16 (v022) — **Тестът с отблясъците е пуснат отново, вече като измерване. Резултат: MIXED.**
  `docs/16` дава критерия, който решава дали повърхността чете като кола или като балон, и той е
  пускан **веднъж**, на 14 септември, срещу preview loft-а: **FAIL, ballooned** — правилно, защото
  тогава нито едно от трите негативни пространства не съществуваше. От v016 насам всичко беше за
  вграждането им в профила, а тестът не беше пускан пак.
  Онзи пуск беше преценка по два matcap кадъра. Този мери.
  **Първият ми опит мереше меша, не повърхността, и остава записан — това е пети път от този вид.**
  Взимах изо-ленти на n·L, намирах свързаните им части и мерех **удължение** (дължина / ширина).
  На loft-нато тяло лицата са подредени в ринги, изо-лента следва един ринг, а лента с широчина едно
  лице има ширина **нула**. Медианното удължение излезе **34 211:1** и присъдата беше PASS. Това е
  топологията, която отговаря, не формата.
  **Какво всъщност отличава балона:** балонът се извива еднакво във всяка посока, а контролираната
  повърхност — не. Това е отношението на двете главни кривини. Близо до 1 е сфера (балон), близо до
  0 е цилиндър (дългият отблясък, защото светлината тича по посоката, която не се извива), и двете
  близо до нула е плоско — другият провал, който `docs/16` назовава.
  **Оценителят се самотества при всяко пускане** — сфера, цилиндър, равнина — защото оценител на
  кривина, за който не е показано, че връща 1, 0 и нищо върху тези три, не е доказателство за нищо.
  (Цилиндърът в първия тест излезе „няма оформени върхове": `bmesh.ops.create_cone` прави **един**
  ринг по оста, всеки връх има три ръба и филтърът за пръстена ги изхвърля всичките. Построен на
  ръка.) Сега: сфера **1.00**, цилиндър **0.00**, равнина плоска.
  **Резултат върху v022:**
  ```
  зона                медиана   цилиндрични %
  нос                   0.27         47
  преден калник         0.42         38     <- най-балонно
  врата и канал         0.30         44
  рамо и intake         0.36         42
  deck и buttress       0.33         43
  опашка                0.27         47
  цяло тяло             0.31         43
  ```
  **MIXED** — не FAIL като през септември, но и не PASS. Тоест: **силуетът вече съвпада с рендера до
  3 mm отпред, а повърхностите са наполовина контролирани.** Това е точно предсказаното в `docs/16` —
  loft през сечения е инструмент за пропорция; качеството на повърхността иска patch-базирана
  работа, която е ръчна.
  Тестът казва, че повърхността държи **посока**, не че посоката е **правилната**, и не вижда дали
  светлината се **пречупва** при трите празнини. Това остава отделен въпрос.

- 2026-09-16 (v022) — **Донорската проверка мереше СПЕЦИФИКАЦИЯТА, не построеното тяло.**
  `check_statev_vs_donor.py` чете `SECTIONS` и `PACKAGE` от скелета и ги сравнява с 986. Това беше
  вярно, докато имаше само скелет, и е грешно сега — и е грешно по същия начин, по който този проект
  вече е грешал: **число, взето от ВХОДА на loft-а, не е факт за ИЗХОДА му.** Пет от характерните
  елементи — страничната линия, гребенът на калника, прагът, фланкът, buttress-ът — съществуват само
  като полета, приложени след сеченията, тоест проверката по спецификация изобщо не ги вижда. Тя
  още докладва, че калникът не короносва предното колело; построеното тяло го короносва от v021.
  Нов `check_donor_fit.py` мери **повърхността, която съществува**, и всяка донорска стойност носи
  произхода си, защото конфликт срещу публикувано число и конфликт срещу оценка от чертеж не са
  един и същ конфликт.
  Две от собствените му мерки бяха счупени и са поправени, преди да влязат в доклад: отворът на
  арката беше мерен **на самата ос**, където срезът вече е махнал материала — тоест мереше дупката,
  не панела, и даваше „130 mm по-широк отвор"; и планът на донора беше мащабиран втори път по mm/px,
  а той **вече е в милиметри** — това превърна донорската полуширина в 6341 mm и клирънса в −5416.
- 2026-09-16 (v022) — **Три корекции за напасване по донора, всичките в рамките на заключеното.**
  - **Предно разширение 6 → 58 mm, спад 620 → 900.** Свито беше на 6 през септември 14, за да падне
    общата ширина до 1850 — но тогава **задницата** беше тази, която стърчеше, и още е тя: 1850 се
    прави от задните ±925. Предницата беше оставена 74 mm по-тясна, отколкото ѝ трябва. Мерено:
    отворът на предната арка стигаше до Y 907.5 при само 851 mm тяло, а външната стена на предната
    гума стърчеше 23 mm пред каросерията при короната на арката. Сега: тяло 875.8, гума **−5.1 mm**.
  - **`FLANK_PULL` 0.88 → 0.95**, защото задното крило е **overlay** и трябва да седи ИЗВЪН OEM
    обшивката с 15–20 mm, а мереше **1 mm въздух** при specX 2107.
  - Лентата на измерването е ограничена до Z 400–630: над това стои стената на среза на кабината при
    |Y| 700 и се четеше като наша обшивка, което даваше −163 mm.
  **И една мярка, която СЪЗНАТЕЛНО не се доиграва.** Въздухът над завареното крило излиза 2 mm при
  искани 15. Донорската полуширина там е оценка от чертеж с **±30 mm**, а цялото място, което
  заключените 1850 оставят на тази станция, е **43 mm**. Разлика, по-малка от собствената грешка на
  числото, не е резултат; донастройването на фланка срещу нея би било напасване на нашата повърхност
  към измерване, което не носи тази точност. Затова има **нова присъда `SCAN`** — не „проблем", а
  „не се решава преди скана".
  **Остават четири CHECK-а, всичките реални:** носът седи 57 mm вътре в донорския бампер (гредата е
  там — скан); предната арка е с 31.7 mm отвор без повърхност, задната с 24.2; предната гума е още
  5.1 mm пред каросерията при короната.
  Заключените непокътнати: **4370 / 1850 / 2415**. Осветление 0, опаковане 0, карта unassigned 0.
  Наслагването срещу рендера **не е регресирало**: средно 15 mm, предница 3 mm.
  Екранът за демонтаж вдига **2 двойки** (прагът минава през следата на предния калник) — следствие
  от разширението, и е екран, не присъда.
  Панелен път презчислен: **124 секции, 15.1 kg, 116 от 124 едно парче; 134 STL файла, 100 264
  триъгълника, 0 извън плота.** Обшивка 12.13 m². Файл `STATEV_001_v022.blend`, хеш
  `5c281ebfcc4a3343`. Откат: v021.

- 2026-09-16 (v021) — **v021: предницата съвпада с рендера до 3 mm, задницата до 21.**
  Наслагването даде цел, срещу която да се работи, и тя е затворена в две стъпки.
  **1. Гребен на предния калник (ново поле `FRONT_CREST`).** Мерено: тялото беше 17–147 mm под
  рендера през целия перед, най-зле при specX −300..0. `z` на полето е снето директно от силуета на
  референцията при калибровката на наслагването; независимо потвърждение, че калибровката е вярна —
  **основата на стъклото в рендера излиза 969 срещу публикуваните донорски 970.**
  Първият опит беше в `SECTIONS` и **не стига до повърхността**: `section_profile` смесва две
  съседни сечения по обединението на техните Z нива и пази само нивата, които **и двете** дефинират,
  тоест гребен на Z 874 в S04 отпада срещу S03, което спира на 774. Измерено — **68 mm късо на
  собствената си станция**. Затова гребенът е **поле**, както страничната линия, прагът, фланкът и
  buttress-ът, и се прилага след смесването, не през него.
  Две подпорки по пътя: пазачът `crown = max(crown, z_top+10)` мереше спрямо **глобалния** връх на
  сечението и вдигаше капака заедно с гребена — сега мери спрямо върха там, където сечението е още
  **широко** (≥ 80% от полуширината); и `resample` подминаваше гребена, защото разпределя точки по
  дължина на дъгата — сега всеки входен връх, който е строг локален максимум по z, се запазва.
  Резултат: гребенът улучва целта **до ±5 mm** на всяка станция, седи навън на Y 315–598 и стои
  **12–75 mm над осевата линия на капака** — тоест калникът и капакът са разделени, както в рендера.
  **2. Deck-ът се държи равен зад задната ос.** Изваждайки гаусовото вдигане, което полето на
  buttress-а вече носи, рендерът иска **deck 878–882 mm на ВСЯКА станция от 2400 до 2900** — равен,
  и равен на 880, което таблицата вече казва при ролбарите. Тоест задницата на рендера е равен deck
  с перка над него, което е точно построената архитектура; нашият просто падаше.
  **Двата възела на 1760 и 2100 НЕ са пипани** — те са в обхвата на предполагаемия обем на сгъване
  (specX 1240–1760) и остават точно където решението от 14 септември ги остави. Всичко променено е
  зад задната ос, където покривът не влиза, а двигателят влиза — и вдигнат deck там му дава повече
  място, не по-малко.
  **Мерено след промяната:** средно отклонение **95 → 15 mm**; предница **3 mm** (най-зле 12);
  задница **92 → 21 mm**. Единственото останало голямо е **specX 2100 (−118 mm)** — точно на ръба на
  блокираната зона, която съзнателно не е пипана.
  **Заключените са проверено непокътнати:** 4370 / 1850 / 2415 и четирите центъра. Проверките минават
  чисто: осветление **0 проблема**, опаковане **0 fault-а** извън `02_BODY`, картата затваря с
  **unassigned 0**, екранът за демонтаж **0 сигнала**. Обшивка 11.57 → **12.01 m²**, ядро 44.9 kg.
  Одит: **10 PROCEED / 2 CONDITIONAL / 11 SCAN REQUIRED / 19 BLOCKED**.
  **Едно нещо е спорно и се казва, а не се ползва:** `P02 HOOD` мина в PROCEED, защото `cowl_x` вече
  не мести границата му — но причината е, че капакът в построеното тяло свършва на **specX 385, а не
  на 420**, тоест 35 mm преди основата на стъклото. Това е въпрос към картата, не доказана
  независимост, затова **P02 не влиза в производството**.
  Откат: **v020**. Файл `02_DESIGN/exterior/STATEV_001_v021.blend`, 9982 върха, 8943 лица,
  хеш `5d887a5cf4b957d7`.
- 2026-09-16 (v021) — **Целият панелен път е презчислен върху v021.** 9 пилотни STL, всеки едно
  парче; **124 печатни секции, ~15.1 kg ядро, 78 972 триъгълника**, 114 от 124 едно парче.
  Център на тежестта specX 1344 (56% назад), Y −3.5 mm.
  **Нерешено и записано:** две парчета от дифузьора (`P22_s08`, `P22_s13`, 32×49×11 и 39×63×11 mm)
  излизат на **2.9 mm над плота** в собствените си файлове. Изпичането на трансформацията в меша не
  ги оправи, причината не е намерена. Слайсърът ще ги свали сам, но това е известен дефект, а не
  чист изход.

- 2026-09-16 (нощ, след края) — **ОТТЕГЛЯМ числата от предния запис: камерата беше огледална.**
  Blender камера на −Y с ротация (90, 0, 0) гледа по +Y, но дясната ѝ ос ляга върху repo +X, което е
  **напред**. Рендърът излизаше с носа надясно — обратно на `ref-05` — и цялата таблица сравняваше
  **нашия нос срещу опашката на референцията**. Оттам „задницата стои 247 mm по-ниско" и „опашката е
  256 mm по-дълга". И двете са грешни.
  Камерата е преместена на +Y с ротация (90, 0, 180). Проверката, която липсваше и заради която
  грешката мина: **рендърът срещу самия меш** на осем задни станции — съвпада до **3 mm**, а обхватът
  е −950..3418, тоест точно заключените 4370.
  **Вярното:**
  - средно отклонение **95 mm** през 16 станции (не 136)
  - **предница, specX −900..+300: моделът е 17–147 mm ПО-НИСКО**, най-зле при
    **specX −300..0 (−144, −147, −129)** — тоест короната на предния калник
  - **задница, specX 2100..3000: 93 mm по-ниско**, не 247
  - **опашка: 297 mm по-дълга** от рендера; носът съвпада до 63 mm
  Поуката е същата като при развъртането: **липсваше проверка, че инструментът мери това, което
  мисля.** Сега тя е в скрипта — рендърът се сверява с меша, преди да се сравнява с картинката.

- 2026-09-16 (нощ, край) — **„Прилича ли на картинката" вече е число.**
  `silhouette_overlay.py` слага модела и `ref-05` в **една координатна система** и мери профила
  станция по станция. Страничният изглед на референцията е достатъчно ортогонален, за да го
  позволи: двата центъра на колела са на **2.2 px** разлика по височина, тоест 6 mm.
  **Калибровка от единственото нещо с публикуван размер** — бронзовите джанти. Пикселите се
  клъстерират на две колела, центърът на всяко се намира с итериран центроид в радиус 105 px,
  разстоянието между тях е междуосието 2415 mm → **2.8753 mm/px**. Земята идва от същите два центъра
  плюс нашите радиуси на гуми и двете оценки се разминават със **7 px = 20 mm** — това е честната
  лента на грешката върху всяко число отдолу.
  **Резултати** (16 станции, където тялото е наше; 11 пропуснати като донорски — рамката на стъклото
  и ролбарите, които `CLAUDE.md` изрично запазва):
  - **Предница, specX под 500: средно 50 mm, най-много 92.** Носът, короната на калника и линията до
    cowl-а следват референцията плътно.
  - **Задница, specX 2100 и назад: моделът стои постоянно 247 mm ПО-НИСКО.** Това е решението от
    2026-09-14, излязло като число: deck 960 → 880 и buttress 1090 → 985, защото височините от
    рендера стоят в обема на сгъване на покрива. Компромисът вече има цена в милиметри.
  - **Дължина:** носът съвпада до **23 mm** (−1013 срещу −990), но опашката на модела е **256 mm
    по-дълга** (3121 срещу 3377). Цялата разлика в дължината е в задния навес. Заключените 4370 не
    се пипат; това е само колко далеч стои заключената стойност от рендера, срещу който е чертана.
  **Две грешни мерки по пътя, и двете уловени, преди да влязат в доклад.** Първият детектор търсеше
  „първия силен вертикален градиент отгоре надолу" и даваше **плосък 1411 mm** през две трети от
  колата — намираше винетката на фона, не колата. Вторият, заливането на фона, беше пуснат върху
  **подплатеното** платно и спираше веднага в черния кант, което даваше същата плоска стойност.
  Работи третото: заливане върху **собствените пиксели на референцията**, толеранс 8 нива —
  гладкият градиент е проходим, ръбът на колата не е.
  **Какво това НЕ може:** сравнява **контури**. Две коли с еднакъв силует и съвсем различни
  повърхности излизат еднакви тук. Разлика, която намира, е истинска; съвпадение не е минаване.
- 2026-09-16 (нощ, край) — **`ref-07` и `ref-08` ги няма в репото.** `CLAUDE.md` ги сочи като
  визуален авторитет, цялата задна архитектура е изведена от тях, а `docs/15` започва с „прати този
  файл заедно с ref-08 инфографиката". В `07_PRESENTATION/references/` има само `ref-01..ref-05`;
  commit `9fd88c3` добавя **описанието** на ref-08, не картинката. Геометрията гони изображение,
  което не съществува тук.

- 2026-09-16 (късна нощ) — **Празните полета стават РЕШЕНИЯ. Деветте доказани панела са нарязани
  за печат.** Досега всяка стъпка спираше на `None` и назоваваше въпроса от `docs/13`. Това беше
  вярно, докато въпросите бяха отворени, и спира работата сега. По изрично указание решенията се
  вземат тук и се коригират на живата кола. Всички са **в един блок** в `panel_production.py`,
  всяко с довод, и всяко на един ред разстояние от истинския отговор на фирмата:
  - **стена 3.0 mm** — принтът е ядрото, стъклото носи; 3 mm при пълни периметри държи 350 mm
    отстояние и не хаби нишка по 11.5 m² кола. Заменя се от Q30.
  - **фланец 30 mm + 1 mm лепилна фуга** — стандартният еднокоренен слепен нахлуп; достатъчна площ
    връзката да е по-здрава от ламината от двете страни. Заменя се от Q15.
  - **фуга между панели 4 mm.** Заменя се от Q16.
  - **плот 350 × 350 × 350 с 10 mm поле** — общият знаменател на достъпните машини. Това е
    най-тежкото предположение тук и е точно числото, което Q26 заменя; пресмятането е едно пускане.
  - **езиче 20 mm вместо щифт.** Първата версия центроваше секциите на **6 mm дюбел** — а през
    стена от 3 mm не минава 6 mm дюбел. Това, което тънка печатана черупка може да носи, е същата
    връзка, която панелите ползват помежду си: едната секция продължава 20 mm отвъд среза, хлътнала
    навътре с една стена плюс лепилната фуга, и следващата ляга върху нея.
  **Кой носи фланеца:** панелът, който се монтира **пръв**; следващият ляга върху него и неговият
  ръб е видимият, тоест регулируемият при напасване. Затова `P28/P41` и `P22` са с голи ръбове —
  насрещният им фланец принадлежи на съседа.
  **Резултат: 125 секции, ~14.1 kg ядро, 81 хиляди триъгълника.** Всяка секция е легнала с
  най-малкия си размер вертикално и стъпва на Z 0. **118 от 125 са едно парче**; седемте, които не
  са, са изброени поименно в `print_schedule.csv`, а не изпратени мълчаливо. 12 секции са под
  60 mm — печатаеми, неудобни за хващане, и приемливи, защото всяка се лепи на място и после се
  ламинира отгоре.
  Три грешки по пътя, всичките от един вид — **геометрия, приписана по център на лице**:
  мрежата на среза не беше рязана, затова секции излизаха над плота; хлътването на фланеца и на
  езичето беше **постоянно**, което ги откъсваше от панела (56 от 66 файла излизаха на части),
  и сега е **рампа** от нула при фугата; а решетка от кутии върху извита черупка хваща несвързани
  петна в една клетка — затова всяка свързана част вече е собствена секция.
- 2026-09-16 (късна нощ) — **Колелата: колко не достигат, за да опрат в каросерията.**
  Мерено на построеното тяло срещу външната стена на гумата при публикуваната следа:
  **отпред** тялото е между −29 и **+27 mm** по-широко от гумата (най-много при specX ±350),
  **отзад** — постоянно **+18 до +23 mm**. Тоест за да застане гумата наравно с каросерията, трябва
  около **0–25 mm на страна отпред** и **20–23 mm на страна отзад**, което е **+0–50 и +40–46 mm
  следа**. Това са нормални дистанционери, но пипат scrub radius, товара на лагера и дължината на
  шпилките — решение на живата кола, не наше. Отворът на арката е по-широк от гумата навътре със
  57.5 mm отпред и 47.5 отзад, значи място в калника има.

- 2026-09-16 (нощ) — **Тялото се реже по граничните равнини, ПРЕДИ да се приписва.**
  Досега картата приписваше цяло лице според центъра му. На меш, чиято тесселация сама по себе си не
  е огледална, това дава панели, които не са огледални: `P28/P41` излизаха 0.142 и 0.099 m², а
  центроидът на `P01` стоеше на 65 mm от осевата линия. **Обемът на тялото обаче е симетричен до
  0.2 литра** (мерено по ленти, обемът не зависи от тесселацията) — значи формата никога не е била
  несиметрична, **приписването е било**.
  `panel_map.split_on_boundaries()` реже тялото по всяка равнина, на която `panel_of` превключва —
  девет по X, четири по Z, пет по Y и огледалата им. После нито едно лице не стъпва върху граница.
  **Ефектът е измерим навсякъде:** обхватите на панелите вече свършват точно на граничните
  стойности (−655, 420, 440, 1635, 1780, 2300, 2000, 3000), а не приблизително; `P21` получава
  0.928 → **1.087 m²** и `P22` дава 0.617 → **0.457**, защото линията Z 330 вече реже точно;
  фугите станаха истински споделени ръбове (`P21↔P22`: 35 точки по цялата ширина).
  **И развъртането се оправи от само себе си.** Преди беше плоско (28.0 при 2 mm и при 25). Сега
  следва смущението: капакът дава 5.3 / 12.1 / 26.4 / 38.0 при 5 / 10 / 25 / 40 mm. Тоест
  количественото оттегляне отпреди беше вярно като оттегляне, но причината е премахната, а не само
  наименувана. Изводът не се мени: където донорска стойност огражда панел, фугата пътува колкото е
  сгрешена оценката, 15–30 mm срещу 15 mm припуск.
  Страничен ефект, изчистен: рязането по равнина, която минава допирателно на плоска геометрия,
  оставя лица с нулева площ. Те се изхвърлят при извличането — иначе се броят като отделни парчета
  (капакът излизаше „3 части", прагът „2").
- 2026-09-16 (нощ) — **Векторите на монтаж в регистъра бяха обърнати на 28 части.**
  Другата половина от manufacturing промпта, записана като неизпълнена от 2026-09-14, вече може да
  се пусне, защото частите съществуват: `exploded_assembly.py` дава маса, център на тежестта,
  разглобен изглед и **екран за правилото на демонтажа**.
  Екранът намери грешката. Регистърът пишеше `+Z` едновременно за сплитера (който се вкарва
  **отдолу нагоре**) и за капака, deck-а, buttress-ите и капака на двигателя (които се спускат
  **отгоре**). Двете не могат да са верни при една конвенция. Същото странично: всяка **L** част
  носеше `+Y`, всяка **R** част `−Y`, тоест прочетено буквално, лявата врата излиза през колата и
  навън от другата страна. Поправено: **8 части по Z, 20 по Y**.
  Резултат: **36 → 0 сигнала.** Правилото „всяка част излиза, без да се маха друга" вече издържа
  екрана. Екранът си остава екран — работи с проекции, не с пометен обем, и не вижда част, която
  се закача чак след завъртане.
- 2026-09-16 (нощ) — **Маса и баланс, първите в проекта.**
  23 части с геометрия, **42.2 kg ядро**. Център на тежестта на каросерията: specX 1328, тоест
  **55% от пътя назад между осите** — за средномоторна кола това е смислено. **Y = −3.7 mm** е
  проверката за симетрия: трябва да е нула и това е колко далеч от нулата са частите в момента.
  Масата е PROVISIONAL (3 mm стена × 1.24 g/cm³, и двете допускания); **балансът оцелява при
  сгрешена плътност, килограмите — не.**
  Кадри: `04_ENGINEERING/statev_v01/exploded/` — страна, отгоре, две 3/4, цветно по части.

- 2026-09-16 (късна вечер) — **Дясната половина се строи като огледало на лявата, не се извлича
  отделно.** Колата е симетрична по построение — всеки заключен размер е симетричен спрямо Y = 0 и
  главните обеми са loft-нати от половин сечения — значи двете половини на двойка са една част,
  отразена. Измерено: осем от деветте двойки съвпадат по площ до 0.4%. **`P28/P41` се разминават с
  30%**, защото границата на сплитера е Z линия на 300, картата приписва по център на лице, а
  булевата операция на устата е тесселирала двете страни различно, тоест едри лица, стъпили върху
  линията, падат от тази страна тук и от другата там. Две части, които се разминават с 30%, не са
  двойка и няма да паснат като двойка.
  Отделното извличане **пак се прави и се сравнява**, за да е симетрията заявена на глас, а не
  приета мълчаливо. Ако формата някога стане нарочно несиметрична, ще излезе точно тук.
- 2026-09-16 (късна вечер) — **`panel_extract` мереше региони и ги наричаше части.**
  Картата отговаря под едно ID за двете страни на колата, значи огледален регион е две части с едно
  име. Мерен като регион, прагът излизаше **0.888 m² и 1841 mm широк** — двойката — и това число
  влизаше в таблица, озаглавена „на част", и оттам към доставчик. Сега всяка част се изрязва от
  своята страна: прагът е 0.445 m² и 269 mm. Общата площ се качва 11.518 → **11.565 m²**, защото
  десният сплитер вече носи своите 0.043 m², които извличането му губеше.
- 2026-09-16 (късна вечер) — **Един файл за доставчика вместо шест.**
  `supplier_package.py` съединява регистъра, архитектурата, производственото определение, одита,
  измерванията и измерената донорска зависимост в **един ред на част, 42 части, 33 колони**. Три
  вида клетка, и разликата е целта: **стойност** (измерена), **PROVISIONAL** (наше работно
  допускане, написано, за да може да се оспори — 3 mm стена и 1.24 g/cm³, от които следва масата),
  и **UNKNOWN Qnn** (въпрос към доставчика с номера си от `docs/13`; нищо не се отгатва, за да се
  запълни такава клетка).
  **За котиране днес: 9 части, 3.782 m² обшивка, ~14.1 kg ядро при провизорната стена** —
  `P01`, `P07/P08`, `P39/P40`, `P28/P41`, `P21`, `P22`. Всяка от тях има изнесен файл, проверен
  байт по байт. Блокиращите въпроси остават четири: Q26 полезен обем, Q30 минимална стена,
  Q32 максимален надвес, Q15 ширина на фланеца.

- 2026-09-16 (вечер) — **Решенията по регистъра са взети. 38 → 42 части.**
  Три от петте несвързани региона се решават днес, защото и двете им половини са доказано наши:
  **`P28 FRONT_SPLITTER` става двойка** `P28 FRONT_SPLITTER_L` + `P41 _R`. Не е разсечен панел, а
  двойка, която никой не е обявил: устата на носа се реже по осевата линия и стига надолу през него,
  така че между Y −144 и 144 няма материал. На страна всяка половина е едно свързано парче.
  **`P07 ROCKER` се дели на праг и ъгъл** — `P07/P08 ROCKER_L/R` (292–2122) и нови
  `P39/P40 ROCKER_END_L/R` (2736–3000, 264 mm, 0.084 m² на страна). Задната арка го сече изцяло,
  не го нарязва. Ъгълът е **собствена част**, а не дял от съсед, защото и четирите му граници са
  наши или публикувани: отворът на арката отпред, станцията на задната маска отзад, линията на
  прага отгоре, подът отдолу. Вкарването му в `P15` би завлякло доказано парче в SCAN REQUIRED
  панел; вкарването му в `P22` би сложило ъгъл от борда вътре в дифузьора.
  **`P19 REAR_DECK` става двойка** `P19 _L` + `P42 _R`. Капакът на двигателя заема центъра на
  deck-а, значи deck-ът никога не е бил едно парче — две ленти, които не се допират. Границата
  между тях е нашата полуширина на капака, не донорска стойност, затова двойката се оправя сега,
  макар формата на deck-а да остава SCAN REQUIRED зад обема на сгъване.
  **`P11 SIDE_INTAKE` и `P15 REAR_HAUNCH` се отлагат съзнателно.** Вторите им парчета са тънки
  ивици на Z 666–702 (0.014 и 0.095 m²), а и двата панела са SCAN REQUIRED — формата им се
  преправя, когато дойде сканът, тоест решение сега би било измислица, не решение.
  **Одит: 9 PROCEED / 3 CONDITIONAL / 11 SCAN REQUIRED / 19 BLOCKED.**
- 2026-09-16 (вечер) — **В изнесеното ядро на `P01` е имало връх на 41 метра.**
  Проверка на bbox-а на всеки изнесен файл: `P01_FRONT_FASCIA_core` носеше върхове на
  specX 41159, Y 9074, Z 4595 — в кола, дълга 4.4 m. Файлът беше иначе валиден: една свързана
  обвивка, 4658 триъгълника, минаваше проверката за свързаност. Само пет от 13974 върха.
  Причина: `use_even_offset` в SOLIDIFY дели на косинуса на половинния ъгъл при всеки връх. Там,
  където повърхността се сгъва обратно върху себе си, косинусът отива в нула и отстъпът в
  безкрайност. `P01` има **шест върха с почти противоположни съседни нормали**, оставени от
  булевата операция на устата.
  `thickness_clamp` **не помага** — измерено на `P01` оставя върха точно където си е при всяка
  стойност и вместо това разбива стената: 2595 cm³ падат на 934 при clamp 1.0 срещу идеални 2685.
  **Решение: отстъп по нормалата, не even offset.** Дава ядро, чийто bbox е bbox-ът на самата
  проектна повърхност (specX −951..−654 срещу −950..−655) и 2413 cm³ — около 10% под идеала,
  което е очакваният недобор от мерене по нормалата вместо перпендикулярно през кривина. За този
  процес това е и правилната стена: **принтът Е ядрото**, стъклото ляга върху външното му лице,
  значи външната повърхност трябва да е точна, а вътрешното лице е това, което отстъпва.
  Добавена е **преграда, не само параметър**: след даването на стена bbox-ът на ядрото се сравнява
  с този на проектната повърхност и ако избяга с повече от две дебелини, файл не се пише.
  Изнесени пилоти: **6** — P01, P28, P07, P39, P21, P22 — всеки проверен байт по байт за една
  обвивка и за габарит вътре в колата.

- 2026-09-16 (най-късно) — **Каросерията никога не е била 16.99 m². 11.52 m² е.**
  Одит по размер на лицата: девет лица над 0.05 m² минаваха през `not_panel()` и се броят за външна
  обшивка — общо **3.95 m² от 16.99**, тоест 32% завишение. Най-лошото е **едно четириъгълно лице от
  1.876 m²**, легнало хоризонтално на Z 640 през целия отвор на кабината — **подът на среза на
  кабината**, приписан на `P09 DOOR_SKIN`, само то половината от обявената му площ.
  Причината е проста и затова е оцеляла: трите правила в `not_panel()` филтрират по `ny`, значи
  капак с нормала по X или по Z минава недокоснат, колкото и да е голям, а надолу по веригата никой
  не гледа площ на лице. Площта изглеждаше правдоподобна, защото никой не я сравни с това какво е
  правдоподобно за кола 4.37 m — 16.99 m² беше твърде много и минаваше.
  Поправката не е списък. Капакът има подпис: **лежи върху равнината на среза, с нормала по оста на
  тази равнина**. Това се проверява срещу режещите тела, които build-ът реално ползва —
  `CABIN` (specX 420–1760, |Y| 700, Z 640) и `ARCH_CUTS` (0/350 и 2415/365, центрове Z 323.5/337.5).
  Отхвърлени: `X_CABIN_FLOOR` 1.876, `X_ARCH_WALL` 1.516, `X_ARCH_END` 1.367, `X_CABIN_END` 0.711.
  Остават две големи лица, и те **са** обшивка: върхът на носа (specX −950) и задната стена
  (specX 3420) — плоски, защото blockout-ът е loft, затворен на крайните си сечения.
  **Следствия:** обшивка 16.987 → **11.518 m²**; маса на ядрото при 3 mm 63.2 → **42.9 kg**;
  `P09` пада 3.736 → 1.860, `P03` 3.249 → 1.622, `P15` 2.765 → 1.390.
  **Класификацията не мърда** — 6/3/10/19 преди и след, значи не е стъпила на завишената площ.
  Още едно следствие: `P09` вече е едно парче (подът на кабината го е „свързвал“), а `P11` и `P15`
  излизат на 2 и 3 парчета, след като стените на арките се махнаха. Регионите, които не са една
  част, стават пет: **P28, P07, P11, P15, P19**.
- 2026-09-16 (късно) — **Изнесеният `P28` STL също беше две парчета. Изтеглен.**
  Проверка байт по байт на четирите пилотни STL (четене на файла, не преизвеждане от Blender):
  `P01`, `P21`, `P22` са по една свързана обвивка, `P28` беше **две**. Устата на носа се реже по
  осевата линия и стига надолу в прага — процепът е Y −144..144. Файлът е изтрит. Веригата вече
  назовава **какво** е разсякло региона, изведено от разположението на процепа спрямо режещите тела,
  а не записано на ръка. Изнесени пилоти: **3**, всичките проверени като едно парче.
- 2026-09-16 (късно) — **ОТТЕГЛЯМ числата от предния запис. Качествено остава, количествено пада.**
  28.0 / 42.5 / 60.1 / 62.3 mm не са изместване на границата. Развъртането ги изобличава: при
  смущение 2 mm P15 дава 60.1, при 40 mm пак 60.1; P02 дава 28.0 при 2 и при 25. Две милиметра не
  местят ръб с шейсет. Това, което се мени, е коя мрежова станция е крайната за панела, а
  надлъжните станции на blockout-а около тези линии са на 36–75 mm. Измерил съм стъпката на
  дискретизацията и съм я нарекъл геометрия — същата грешка като оттеглените „42 mm стъпка“,
  „80/66/65/80 по вратата“ и „4.5 mm над арката“. Пети път от същия вид.
  **Вярното няма нужда от мрежа.** Където донорска стойност огражда панел, фугата **е** равнината на
  тази стойност, значи пътува точно толкова, колкото е сгрешена оценката: 15–30 mm по лентата, която
  `cage_986.py` записва, срещу 15 mm припуск. Изводът „условните не стават при 15 mm“ **остава**,
  но исканият припуск е ~30 mm, не ~45.
  Заменя `boundary_sensitivity.py` с `donor_exposure.py`, който отговаря на двоичния въпрос —
  огражда ли тази стойност този панел — и носи развъртането в себе си, за да не изгние твърдението.
- 2026-09-16 (късно) — **Грешка в одита, поправена: срезовете на арките не зависят от донора.**
  `INFLUENCE` в `manufacturing_audit.py` пишеше, че вертикалният център на среза идва от tyre OD,
  който е approx. Не идва. Идва от `ARCHES` tod 647 и 675 = 19×25.4 + 2×235×0.35 и
  19×25.4 + 2×275×0.35 — аритметика върху гумите, които **ние** избрахме, точна до 0.1 mm.
  `DIMS["tire_od"]` не се чете нито от `statev_master_volumes.py`, нито от `statev_skeleton.py`,
  нито от `panel_map.py`. Цялата геометрия чете **четири** донорски стойности: `cowl_x`, `hoop_x`,
  `track_front`, `track_rear` — последните две публикувани. Понеже `classify()` търсеше подниза
  APPROX, „PUBLISHED+APPROX“ пращаше в CONDITIONAL всеки панел, до който стига арка — заради число,
  което геометрията не чете.
- 2026-09-16 (късно) — **`P07/P08 ROCKER` минава CONDITIONAL → PROCEED, с доказателство.**
  Двоичното измерване: нито една от четирите approx стойности не мести границата му. Причината е в
  `panel_of()` — прагът се връща при `z < rocker_top` във всеки X клон, тоест местенето на фугата
  сменя през кой клон минава, но не и отговора. Границата му е наша Z линия. И втората половина е
  чиста: срезът на кабината (единственото, което `cowl_x` и `hoop_x` преоформят) е кутия specX
  420–1760, |Y| ≤ 700, Z ≥ 640, а прагът стига до Z 355 — **нула лица** в кутията, с 30 mm запас.
  Одитът става **6 PROCEED / 3 CONDITIONAL / 10 SCAN REQUIRED / 19 BLOCKED**.
- 2026-09-16 (късно) — **Две находки от веригата на P07, и двете структурни.**
  1. `panel_map` работи с |Y|, значи огледален регион отговаря и за двете страни и `extract()`
     връщаше двойката като един обект. Първият изнесен P07 STL съдържаше **двата** прага, кръстен
     `_L`. Изтрит. Веригата вече реже по знака на Y — +Y е ляво по конвенцията на репото, това е
     факт, не избор. Предишните четири пилота са централни панели и не са засегнати.
  2. **`P07` не е една част.** Задната арка сече прага изцяло: центърът ѝ е на Z 337.5 с радиус 365,
     тоест срезът слиза под нулата. На страна остават **две** парчета — основен праг
     specX 284–2122 (0.560 m²) и опашка 2708–3000 (0.127 m²) зад задното колело. Веригата спира и
     не изнася STL, защото регистърът води P07 като една част, а една част не се печата на две.
     Три възможни отговора, и всичките са решение на регистъра, не на скрипта: да се раздели
     записът; опашката да влезе в съседния панел, чието продължение всъщност е; или да се направи
     мост над отвора, което мени картата.
  **Следствие:** PROCEED са 6 панела, но изнесени пилотни STL остават 4. P07/P08 чакат решение на
  регистъра, не скан.

- 2026-09-16 — **„CONDITIONAL" е измерено и излиза 42.5 mm срещу 15 mm припуск.**
  `boundary_sensitivity.py` мести всяка приблизителна донорска стойност с ±25 mm — средата на
  записаната лента ±15–30 — и мери колко се измества границата на всеки условен панел. Резултат:
  капакът 28.0 mm при `cowl_x`, калникът 18.6 mm при `cowl_x` и **42.5 mm при `door_front_x`**.
  Всяко от тях надхвърля провизорния припуск от 15 mm.
  **Следствие:** петте условни панела **не могат** да се строят днес при 15 mm припуск. При около
  45 mm биха могли — но 45 mm подрязване върху ламиниран панел е решение на композитната фирма, не
  наше, и влиза в `docs/13`.
  Измерена е само половината. `cowl_x` и `hoop_x` поставят и cabin cutter-а, тоест ако мръднат,
  мени се **самото тяло**, не границата — и никакъв припуск не покрива това. Другата половина иска
  по едно построяване на смущение.
- 2026-09-16 — **Находка: `P07/P08 ROCKER` не зависи от фугите на вратите.** Одитът го класира
  CONDITIONAL по припокриване на X обхвати, което е груб тест. Измерването показа нулево изместване,
  а прочитът на `panel_of()` обяснява защо: прагът се връща при `z < rocker_top` във всеки X клон,
  така че местенето на `door_front_x` или `door_rear_x` сменя през кой клон минава, но не и
  отговора. **Границата му е наша Z линия.** Остатъчната му изложеност е само към срезовете на
  арките, чиито X и Y са публикувани, а вертикалният им център идва от диаметъра на гумата —
  приблизителен. Това не е мерено и не е основание да се прекласира още.

- 2026-09-15 (v020) — **Профилът се уплътнява по Z ПРЕДИ прилагането на character полетата.**
  `PROFILE_STEP = 6.0`. Това е поправка на грешка, която тихо е намалявала всяко поле в скрипта,
  откакто полетата са написани. `section_profile()` връща само около шест контролни точки на
  сечение, през 130–150 mm. `character()` се оценяваше само в тях, така че поле с 10 mm преход —
  ръбът на прага — се семплираше на Z 250 и после на Z 400, а лофтът теглеше права между тях.
  34-те милиметра излизаха като 150 mm рампа без ръб и се мереха като **0.0 mm стъпка**.
  Обяснява наведнъж поредица неща, които изглеждаха несвързани: защо `trans` на undercut-а беше
  инертен и на 18, и на 72; защо `w` и `n` на канала не променяха нищо; и защо вдигането на
  `N_HALF` никога не помагаше — то преизчислява готовата полилиния и не може да върне детайл,
  който полилинията никога не е носила. Семплирането трябва да е гъсто там, където се оценяват
  **полетата**, не там, където се преизчислява резултатът.
  Измерено при specX 1200, при непроменени заключени размери: стъпката на прага от нечетима на
  **30.2 mm** при зададени 34; входът на канала от нечетим на **115°**; хордовата дълбочина на
  канала от **69.9 на 99.0 mm**. Стойност 6 mm, защото 12, 6 и 3 дават еднакъв отговор до половин
  градус. Короната мърда максимум 0.88 mm при лимит 1.2. Откат v019.
- 2026-09-15 — **Метриките за повърхност се записват като инструмент** (`surface_metrics.py`),
  защото три от четирите използвани досега дадоха грешни отговори, които оцеляха, докато някой не
  измери същото по втори начин. max-minus-min чете нула, когато фланкът зад празнината пада.
  Бинване по Z през 30 mm инжектира ±15 mm шум. Ъгълът между съседни семпли мери къде са попаднали
  семплите — същият гребен даде 55, 56, 38 и 21 градуса при 60, 120, 240 и 480 точки. Работят:
  **crease_angle** за смяна на посоката, **step_height** за отместване с успоредни фланкове —
  прагът е точно такъв и crease е сляп за него — и **chord_depth** за дълбочина на празнина.
  Имат самотест върху синтетични сечения. Последната проверка не е метрика, а рендер с твърда
  светлина: числата казват дали ръбът съществува, само рендерът казва дали се чете.

- 2026-09-15 — **Горнището на предния калник се приема на ~763 mm като работна линия, не като
  замразена стойност.** Основанието е измерено: A/B тестът дава под от 730 mm за архитектурата, не
  поисканите 715, и стигането дотам струва 64 mm при A-стълба за 33 mm калник. При B преходът
  калник → A-стълб спира да прелива. Не се кодира локално поле `fender_top` — то би било правилният
  начин да се разделят двете линии, но A/B-то показа, че по-нисък калник изглежда по-зле, тоест
  полето решава проблем, който не съществува. Връщането остава два параметъра.
- 2026-09-15 — **Началото на страничната линия иззад предната арка се приема.** Алтернатива няма:
  арката е цилиндър с радиус 350 върху оста и при specX 330 вече е махнала собственото долно рамо
  на канала. Дълбочина, поискана там, не може да се покаже от повърхността. Причината е геометрична,
  не стилистична.
- 2026-09-15 — **Въведено е правилото за частичен checkpoint** (`docs/17`). Контролна точка може да
  се минава по зони, когато един критерий е блокиран от липсваща информация, а не от незавършена
  работа. Три условия. Приложено веднага: **CHECKPOINT 01 минава за FRONT, SIDE и REAR по профила;
  ROOF остава BLOCKED** до сесия S2 от плана за сканиране. Минаването на контролна точка **не е
  freeze** и нищо не е замразено.
- 2026-09-15 — **Осемте предложени части са приети и влизат в регистъра като P28–P38.** Регистърът
  расте от 27 на 38 части, редът на сглобяване от 18 на 26 стъпки. Всяка е обоснована от рендера
  или от закон: сплитер, който пръв опира в земята и трябва да се сменя сам; рамки на светлинните
  острия, които са друг материал и друг доставчик от модулите; видима структура в страничния вход;
  ламели на капака, които като отделна част се печатат плоско; централна маска с надписа; обков на
  ауспусите, който вижда топлина; гнездо за номер; капаци на огледалата.
  **Една корекция при приемането:** капаците на огледалата влизат с `SHAPE_DRIVER = DONOR`, не
  `OURS`. Капак върху OEM огледалото трябва да пасне на корпус, който никой не е мерил. Това
  поправя твърдението в `docs/11`, че капакът е част, независеща от донора — не е.

- 2026-09-15 (v019) — **`REAR_UNDERCUT_FIELD.xb` 3150 → 2950.** Избрано от контролиран A/B/C
  експеримент върху точната v018 база, в който xb беше единствената разлика между вариантите.
  Причина: undercut-ът беше четири пъти страничния канал и в заден профил тялото четеше като
  стъпило върху отделна долна структура. 3050 отхвърлен като недостатъчен, 2850 като прекалено
  рязък (58 mm най-голяма разлика между съседни станции срещу 50 в самия v018). **Стойността е
  визуален избор, не изведена** — казано изрично, за да не я приеме бъдеща сесия за изчислена.
  Единствените измерими котви в зоната са край на задната арка 2780 и начало на дифузьора 3145.
  Заменя оттеглената препоръка `xa` = 2780, за която е експериментално доказано, че не прави нищо:
  между 2560 и 2780 лентата на undercut-а е вътре в отвора на задната арка, тоест тялото там вече
  е изрязано, и промяната мери най-много 1 mm на станция.
- 2026-09-15 — **Работим без да чакаме скана; сканът става последен fitting етап.** Причина:
  донорът може да дойде след седмици, а нашата форма не зависи от него. Това не е нов курс —
  `CLAUDE.md` вече записва „Design before donor". Твърдото правило, което прави стратегията евтина:
  **монтажният интерфейс се строи последен и отделно от кожата на панела** — фланци, болтови шарки
  и допирни повърхности не се вграждат във формата, а са отделна регенерируема стъпка. Плюс
  твърдо ограничение №3: 15–20 mm въздух до всяка OEM структура, докато няма скан. Панели, чиято
  ФОРМА се управлява от донорски hardpoints — предни калници, врати, задни четвъртини — ще мръднат
  след скана; нос, капак, външна кожа на deck-а, дифузьор и капаци на огледала са почти без риск.
  Затова се строи в този ред.

- 2026-09-15 (v018, корекция) — **ОТТЕГЛЕНА ОБОСНОВКА.** Записът от същия ден по-долу
  („Излишъкът на предния калник е намален до +85 mm, не върнат на 678") се позовава на това, че
  короната на предната арка е Z 673.5 и че при 678 оставало почти никакво тяло над отвора. **Тази
  обосновка е грешна и се оттегля.** Тя вади височината на арката при specX 0 от височината на
  тялото при specX 350 — две различни позиции по X, а разлика между две станции не е локална
  дебелина. Предната арка е цилиндър с радиус 350 върху оста и затваря при specX ±350, тоест при
  specX 350 няма отвор. Измерено на едно място, при specX 0: v016 = 700.4, v018 = 702.9.
  **Решението остава същото**, но по друга причина: v016 е приблизително плоска около 700 mm с
  31 mm хлътване при specX 300, а v018 дава монотонно изкачване. Историческият запис се оставя
  непроменен нарочно, защото по него бяха давани указания. Виж `04_ENGINEERING/reports/v018_report.txt`.
- 2026-09-15 (v018) — **Каналът на вратата не е пипан, защото „хлътването" беше моя грешка в
  измерването.** Докладваните 80/66/65/80 при specX 715–820 идваха от бинване на Z през 30 mm
  преди вземането на референтното рамо; границата на кофата пада различно на всяка станция и
  инжектира около ±15 mm шум. Полето иска монотонни 126.8/126.9/120.9/115.6, а точното измерване
  върху самите ринги — които build() записва като върхове — дава съседни разлики 2 и 4 mm там,
  най-малките на колата. Заменя указанието „причината е в depth table, а не в метода".
  Записано, защото по него беше дадено указание за корекция.
- 2026-09-15 (v018) — **Фасетата под устата на носа е от cutter-а, не от тялото.** Тялото носи
  сечения през 17–33 mm, четиринадесет през устата. Cutter-ът беше правоъгълник с четири ъгъла,
  лофтнат през шест станции на до 80 mm — кутия. Преизчислен на 12 mm през Catmull-Rom и с
  радиус 26 mm по двата вътрешни ъгъла. Заменя указанието „boolean върху зона с недостатъчно
  сечения". Позиция, дълбочина и височина на устата непроменени.
- 2026-09-15 (v018) — **Ръбът на deck-а е две контролни точки, не една.** Гребенът беше една
  точка и 60-точковият resample я заобляше, а под нея стоеше 200 mm прав диагонал под 45°.
  Сега: кратък стръмен подход, после гребен. Спадът е ограничен до 60 % от наличната височина
  над върха на сечението, защото фиксирани 34 mm просто се пропускаха там, където buttress-ът е
  нисък. По-остър на 5 от 8 станции, корона ±0.69 mm при лимит 1.2.
- 2026-09-15 (v018) — **Излишъкът на предния калник е намален до +85 mm, не върнат на 678.**
  Короната на предната арка е на Z 673.5; 678 оставяше 4.5 mm тяло над отвора, тоест изобщо
  нямаше горнище на калника. Още v015 беше разширил предницата, за да има обем над арката.
  Измерена цена: процепът при A-стълба 114 → 134 mm, като от specX 1000 назад линията не мърда.
  Все още неодобрено — стои в `docs/14`.

- 2026-09-14 (v017) — **Горнището на предния калник се вдига със 161 mm при specX 350 (678 → 839)
  като последица от пренасянето на beltline-а напред.** Причина: горнището на калника и горнището
  на вратата са една и съща линия и тя трябва да пристига в A-стълба на височината на колана, а не
  да стъпва на нея; `CABIN_X0` е изнесен на 385, после на 285 с преход `CABIN_RAMP` 155, защото
  ринговете се изглаждат по X в прозорец от около ±100 mm и рафт, който се появява рязко на
  основата на стъклото, се осреднява с cowl-а пред него. Короната на капака по осевата линия не е
  мръднала (925 срещу 922). **Това е промяна в заключена визуална зона и НЕ е одобрена** — вписана
  в `docs/14`, чака преглед. Двете числа за връщане назад са `CABIN_X0` и `CABIN_RAMP`.
- 2026-09-14 (v017) — **`BELT_Z` заменя `BELT_LIFT`: абсолютна проектирана линия на горнището на
  вратата вместо добавка към върха на сечението.** Причина: `BELT_LIFT` слагаше точка на 94 % от
  горната полуширина, тоест **навътре от cabin cut-а при Y 700**, и срезът после махаше точно това,
  което трябваше да стане beltline. Освен това при specX 440 сечението се прибираше на Z 732 и
  оттам вървеше като един прав конус до короната — нямаше горнище на вратата, което да се мери.
  Сега фланкът се държи почти вертикален до `BELT_Z` и сечението се прибира едва там. 115 mm е
  третирано като **ориентир, не цел**: наложено навсякъде би изравнило кабината, а плавно
  изменящият се процеп е това, което чете като проектирано.
- 2026-09-14 (v017) — **Хордовата дълбочина е мярката за канал, не max-minus-min.** Колко навътре
  стои повърхността спрямо правата, свързваща собствените ѝ рамене на ±120 mm. Причина:
  max-minus-min дава нула винаги когато фланкът зад канала пада, което е по цялата дължина на тази
  кола, и затова съобщаваше „няма канал", когато имаше, и мълчеше, когато boolean тихо не беше
  направил нищо. По новата мярка v016 излезе **отрицателен** почти навсякъде — повърхността се
  издуваше там, където трябваше да е каналът.
- 2026-09-14 (v017) — **Празнините са непрекъснати полета в профила, а не лофтнати cutter-и през
  boolean.** Суперкаусиан по Z върху осева линия, умножен по обвивка, която навлиза, ДЪРЖИ и
  отпуска; `smoothstep` има нулев наклон в двете колена, така че няма стъпка между станции по
  конструкция, а не по случайност. Прилагат се **последни**, след всичко, което разширява, защото
  character полетата ги издърпваха обратно навън. Заменя `FENDER_CHANNEL`, `DOOR_CHANNEL` и
  `REAR_UNDERCUT` таблиците. Предният канал и каналът на вратата са **едно поле**: като две
  оставяха мъртва зона от 18 mm при specX 550. Линията започва от задния ръб на предната арка, не
  над колелото — цилиндърът на арката вече е махнал **собственото ѝ долно рамо** при specX 330.
  Booleans: от 15 на 6. Остават само кабината, четирите арки и устата на носа — неща, които не
  могат да излязат от профил на сечение.
- 2026-09-14 (v016) — **Buttress-ът е profile geometry, а не boolean union.** Стъпил е на
  `DECK_SPINE` с гаусова височина с връх на задната ос, така че haunch, blade, deck и опашка са
  една лофтната повърхност. Причина: два последователни union-а, а после и един двучерупков union,
  сриваха меша; освен това форма, която принадлежи на тялото, трябва да идва от loft-а, а не да се
  лепи отгоре. Това премахна и стъпката от 201 mm в задната корона при specX 2800.
- 2026-09-14 (v015) — **Указанието за „208 mm cowl cliff" е оттеглено, защото диагнозата беше моя
  грешка.** Сравнявах короната на капака по осевата линия (specX 420) с горнището на вратата до
  отвора на кабината (specX 800) — две различни неща; cabin cutter-ът маха всичко над Z 640 при
  |Y| < 700 през целия този участък, така че двете отчитания никога не са били една и съща
  повърхност. Реалният проблем беше друг: beltline 820 срещу основа на стъклото 970. Записано тук,
  а не тихо изхвърлено, защото по него бяха давани указания.

- 2026-09-14 — Скриптовете не падат заради преименуван елемент. `statev_skeleton.py` се чупеше
  тихо на `next(...)` за изтрит "HEADLIGHT" и строеше сцената наполовина — без root, камери,
  материали и debug. Търсенето на елемент вече връща None и съответният warning просто не се
  задейства. Всяка сесия почва с пускане на трите скрипта подред и проверка за FAIL.
- 2026-09-14 — Панелите се произвеждат от външна композитна фирма (BG), тя шкури и боядисва.
  Следствие: `docs/13` се попълва преди панелната архитектура — build volume, формат, дебелина,
  допуск, фланец и монтаж идват от тях, не от нас. Правилото „един панел първо“ се казва изрично.
- 2026-09-14 — Одобреният рендер е визуален авторитет; CAD не преработва дизайна. Заключените
  елементи са в `docs/14`. При инженерно ограничение се прави най-малката възможна промяна и се
  записва там. Стоповете са лента **И** L-образни краища, не едно от двете.
- 2026-09-14 — Deck-ът е BLOCKED до скана. Правилото е: обем на оригиналния покрив първо, deck
  около него после. Височините от рендера са максимум, който дизайнът иска, не проверена стойност.
- 2026-09-14 — Повърхността НЕ се строи с един глобален loft през сеченията. Строи се като мрежа
  от patch-ове със зададена непрекъснатост (`CONTINUITY`). G2 не се гони навсякъде — това прави
  моделите сапунени. `statev_preview_loft.py` нарушава правилото по конструкция и затова е само
  инструмент за пропорция; хвърля се, когато започне сърфирането, не се доразвива.
- 2026-09-14 — Работи се по зони, не панел по панел до готов панел — последното води до
  разминавания между панелите. Пет етапа, четири контролни точки (`docs/17`).
- 2026-09-14 — FREEZE правилото е код, не конвенция. Замразена зона спира строежа при промяна.
  Размразяването иска причина и се записва. Не се заобикаля чрез ръчна промяна на state файла.
- 2026-09-14 — Приета йерархия за решения: A донорски hardpoints, B клирънс, C пропорции,
  D повърхност, E въздушен поток, F производимост, G предварителни размери. CAD страната взима
  решенията сама и показва резултат; собственикът преценява пропорции, не числа.
- 2026-09-14 — Толерансът ±30 mm на чертежа важи за скелета, но НЕ около колело, фуга или
  отвор — там 7–24 mm стават реален панелен проблем. Проверката ги разделя.
- 2026-09-14 — `CAGE_986` не се дублира в `STATEV_001`. Колекция `00_DONOR_HARDPOINTS` съдържа
  само указател: донорската референция стои отделно от STATEV геометрията, един източник на истина.
- 2026-09-14 — `.blend` версиите са инкрементални в `02_DESIGN/exterior/` (v001, v002, …) и се
  commit-ват само от local. Сцената се регенерира от скриптовете; `.blend`-ът е снимка, не източник.
- 2026-09-14 — STATEV 001 dimensional spec v0.1 приет като работна база
  (`docs/10-statev-001-spec-v01.md`). Изпълнява се от `statev_skeleton.py`; проверява се от
  `check_statev_vs_donor.py`. Конвенцията на репото (+X напред) остава — spec X се обръща на
  входа. DIMS target_length 4400 → 4370, target_height 1260 → 1285.
- 2026-09-14 — Редът е фиксиран: sections → rails → визуална проверка от собственика → loft.
  Без loft преди одобрен wireframe. Числата се коригират в `SECTIONS`, не в mesh-а.
- 2026-09-14 — Три fatal конфликта в v0.1 се решават в полза на донора, не на спецификацията:
  главини Y (следата е фиксирана), фар (стърчи 215 mm извън каросерията), страничен intake
  (не се припокрива с реалния отвор). Виж доклада в docs/10.
- 2026-09-09 — 4-изгледният чертеж се обработва само програмно (екстрактор → JSON →
  блок). Стойностите от него са `approx` ±30 mm и никога не се „подобряват“ на ръка —
  следващата точност идва от скана. `hoop_y` = 350 approx (не TODO).
- 2026-09-09 — 986 не се моделира ръчно от blueprints (артистични контури, ±20 mm,
  20–40 ч. труд за underlay). Вместо това: `01_CAD/scripts/block_986.py` строи
  приблизителен обем (профил от чертежа × ширина 1780 × предположен план/сечения,
  ±30–50 mm) в колекция `UNDERLAY_986` — само за обем/пропорции. Точен 986 mesh
  (€10–20 от CGTrader/TurboSquid) е опция, не приоритет; реалната повърхност идва от скана.
- 2026-09-09 — Чертеж от the-blueprints.com (€22) **не се купува**. Безплатни
  източници покриват нуждата: официален workshop manual (Group 0a размери, Group 5
  structure dimensions) + CC BY профил от getoutlines.com, калибриран по междуосието.
  Manual-ите стоят локално извън git (`04_ENGINEERING/reference/manuals/`, .gitignore).
- 2026-09-09 — Числа в клетката по manual-а: междуосие 2415 (не 2416), дължина 4315
  RoW, клирънс 95 RoW. Hardpoints стъкло/hoop/врати/intake от чертежа са `approx`
  (±15 mm) до скана; `hoop_y` остава TODO.
- 2026-09-09 — Cage screenshots (ортографски профил/отгоре) живеят в
  `04_ENGINEERING/cage/`; клетката не се пази като `.blend` (регенерира се от
  `cage_986.py`). Image-to-3D (Hyper3D/Hunyuan) се ползва най-много като 3D
  скица-подложка в клетката — никога като геометрия за печат.
- 2026-09-08 — Два режима на работа: **remote** (claude.ai/code) само за docs и
  скриптове, слива се в `main` в края на всяка сесия; **local** (Mac + Blender
  MCP) за всичко в Blender, `git pull` преди / `git push` след. „Current status“
  в `CLAUDE.md` се обновява при всяка сесия. Правилата са в `CLAUDE.md`.
- 2026-09-08 — Репо `drdomayer/vehicle-3d-modeling` създадено; работата минава
  към Claude Code + Blender MCP. Blender 5.2 + MCP addon инсталирани.
- 2026-09-08 — Дизайнът се прави от собственика в Blender; без нает surface
  дизайнер (няма бюджет; иска да научи 3D за печат преди да купи голям принтер).
  Резервен вариант: базови повърхности от freelancer за €1–2k.
- 2026-09-08 — Започва се преди донора: hardpoint клетка + чертеж + артистичен
  модел като подложка, 15–20 mm буфер, подравняване по скана после.
- 2026-09-08 — Светлини: E-marked Hella модули (90 mm bi-LED, LEDayFlex,
  Shapeline) в принтирани корпуси; лещите не се пипат.
- 2026-09-08 — Футуристична посока (ref-05) избрана; modern-retro отпада.
- 2026-09-08 — Мекият покрив остава; deck-ът се вдига, за да "приеме" затворения
  покрив; hardtop = евентуална фаза 2.
- 2026-09-08 — Донор: 986 Boxster 2.5/2.7 ≤ €6k (2003–04 предпочитан). Z4 E86
  и E46/E36 отхвърлени — front-engine + запазен greenhouse се чете като донора.
- 2026-09-08 — One-off без калъпи: стъклопласт директно върху принтирания
  мастер. Калъпи само при кит.
- 2026-09-08 — Задни квартери: overlay върху OEM обшивка (~70%) + flush секция с
  фланец (~30%); структура не се реже.
- 2026-09-08 — Двигател 2.7 остава за фаза 1; 450 hp = фаза 2.
- 2026-09 (ChatGPT master summary) — Марка STATEV, първа кола STATEV 001; workflow
  scan → CAD → print → composite; интериор с готова електроника; без custom CAN.
- 2026-08 — Вардарец (986, modern-retro) — предишна концепция, вече заменена.
